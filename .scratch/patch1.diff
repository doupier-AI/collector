--- a/apps/api/src/service.ts
+++ b/apps/api/src/service.ts
@@ -494,6 +494,46 @@
   }
 }

+function materialTitle(c: CaptureRecord): string {
+  return c.sourceTitle || (c.content ?? "").slice(0, 80).replace(/\n/g, " ") || c.sourceUrl || "(无标题)";
+}
+
 export function checksumCapture(input: CaptureInput): string {
