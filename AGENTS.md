# AGENTS.md

本文件保存适用于 Collector 全部任务的长期约定。验证执行、真实模型验收、并行开发、Issue 操作、浏览器验收等专项规则按需读取 `docs/agents/` 下对应文档，路由见文末「Agent skills（专项规则路由）」。

## 用户沟通

- 默认使用中文。
- 用户不具备开发知识，先说明用户可见结果，再解释必要的技术原因。
- 使用产品语言描述界面、流程和风险；命令与内部模块名称只在排查需要时出现。
- 需要用户选择时，给出少量差异清楚的方案和推荐理由。

## 项目快速地图

Collector 是 Windows 本地优先的 AI 学习与研究应用，使用 Node.js 24+、npm 11+、TypeScript npm workspaces。正式产品是 React 19 + Vite WebUI、Node 本机 API、SQLite 与外部模型/搜索适配；仓库没有 `pyproject.toml` 或 `requirements*.txt`，不要为普通任务引入第二套 Python 应用栈。

### 目录、职责与依赖

- `apps/web`：React WebUI、客户端状态、HTTP/渐进事件客户端、Vitest 组件测试和 Playwright 端到端测试；其生产构建由 API 同源提供。
- `apps/api`：loopback HTTP/SSE、领域服务、SQLite、文件解析、任务调度、认证、启动器与 Web 静态资源交付。
- `packages/capture-contracts`：Web、API、测试与模型层共用的领域和接口契约；名称保留历史字样，但它是现行全产品共享契约。
- `packages/model-gateway`：外部模型、原生联网与供应商能力适配，只通过共享契约与上层交互。
- `tests`：服务端、持久化、HTTP、恢复、模型与共享契约的 Node 单元/集成测试。
- `scripts`：门禁、启动、日志包装、视觉审阅和专项探针；`docs` 保存现行事实、ADR、架构与专项执行规则。

依赖方向固定为：`apps/web → capture-contracts`、`apps/api → capture-contracts + model-gateway`、`model-gateway → capture-contracts`；`apps/api` 负责组合运行时并提供 `apps/web/dist`。跨越这些方向的修改先检查所有调用方与测试。

### 常用命令

- 安装：`npm.cmd install --cache .npm-cache`
- 正式启动：`npm.cmd run launch`；确定性演示：`npm.cmd run launch:mvp`
- 开发：`npm.cmd run dev:api`；需要前端热更新时另开 `npm.cmd run dev:web`
- 构建：`npm.cmd run build`
- 定向检查：`npm.cmd run check`、`npm.cmd run test:web`、`npm.cmd test`、`npm.cmd run test:e2e`
- 全量门禁：`npm.cmd run gate`；只按下文分级规则或合并要求运行，不把它当作每次小改的默认命令。

### 架构边界与受保护区域

- WebUI 只经统一客户端访问 HTTP/SSE，不直接读取 SQLite、文件系统或模型凭证；API 和领域服务是业务写入与恢复入口。
- 正文是内容唯一事实源，切片、语义范围、导航和引用均为派生或稳定定位层；不得恢复正文副本或在 WebUI 维护第二套业务事实。
- API Key、会话令牌、认证头和启动器控制凭据留在既有凭证与认证边界内，不进入普通日志、浏览器持久化、URL、业务导出或 Agent 回传。
- `node_modules/`、`dist/`、`dist-tests/`、`.collector-data/`、`.npm-cache/`、`logs/`、`test-results*/`、`e2e-artifacts/` 是依赖、生成或本机运行产物，不手工修改或纳入任务提交。
- `package-lock.json` 只随明确的依赖变更由 npm 更新；SQLite 迁移、`LATEST_SCHEMA_VERSION`、共享契约、认证安全、Playwright 基础设施和构建配置属于高影响区域，修改前后必须检查上下游并按规则升级验证。
- 视觉基线 PNG 只在已确认的视觉变化中更新；强制重拍后逐张审阅差异，不能用新基线替代行为正确性的证明。

## 新概念、新功能与新边界强提醒

- 任何产品或边界讨论只要涉及新增概念、功能、关系、状态、权限、自动化、默认行为或其他此前未被用户充分讨论并明确确认的内容，必须先中断正常推演，以醒目的“⚠️ 新概念 / 新功能 / 新边界提醒”主动告知用户；不得把它夹带在示例、术语替换、方案说明、相邻问题或总结中静默引入。
- 强提醒必须直接说明：准备引入什么；它此前属于“从未提及”“仅模糊提及”“只有历史实现或内部术语”“候选方案”还是“已有明确产品决定”；为什么现在需要讨论；它与现有概念有什么区别；会给用户带来什么可见行为与长期约束；不引入时会怎样；有哪些主要替代方案和推荐理由。
- 历史代码、旧文档、旧 Issue、内部字段、已有界面、模型行为或过去含糊的聊天内容，只能证明“曾经存在某种实现或说法”，不能证明用户已经批准其产品含义。不得以“以前已经有了”“当前系统这样实现”或“相邻决定似乎依赖它”为理由，把未充分讨论的内容默认为既定边界。
- 在用户明确确认前，新内容只能标记为“待确认”或“候选方案”；不得写成现行共识、稳定术语、已确认边界、实施任务或验收标准，也不得据此继续推导下游决定。一次只确认一个决策分支，不能用对整体方向的确认替代对其中新概念的单独确认。
- 如果发现现行文档已经把未经充分讨论的内容写成共识，必须立即明确提醒用户并暂停依赖该内容的讨论或实施；先把它恢复为待确认状态或记录争议，待用户明确裁决后再更新基线。不得通过换词、补解释或继续提问，使既成文档事实潜移默化地代替用户决定。
- 代理必须主动执行上述披露义务，不等待用户追问“这是什么”“什么时候决定的”或“为什么需要它”。是否属于新引入拿不准时，一律按新内容强提醒处理。

## 文档与事实源

产品和实施决策按以下顺序读取：

1. 根目录 `AGENTS.md`：长期沟通、文档、提交和验证规则；
2. `docs/PROJECT.md`：唯一现行事实入口——产品定义与核心路径、正式基线与迁移期候选、四态能力表及证据、活动父 Issue 与阻塞、当前限制与待产品决定、有效 ADR 索引；
3. `docs/adr/`：关键、难逆转、有真实取舍的决策留痕；与旧文档冲突时以 ADR 与 PROJECT.md 为准；
4. `CONTEXT.md`：纯领域词汇表，只定义术语，不描述实现状态；
5. `docs/ARCHITECTURE.md`：技术边界；
6. `apps/web/IMPLEMENTATION.md`：WebUI 当前契约、客户端状态、安全和前端验证要求；
7. `docs/HUMAN_ACCEPTANCE_STANDARD.md`：人工验收要求。

- 每次实施任务开始时先读取 `docs/PROJECT.md` 了解当前基线、能力状态、活动父 Issue 与待产品决定，并按当前任务涉及的 ADR、架构或验收章节加载必要上下文；不要默认重读全部长期文档。读取 `docs/PROJECT.md` 按节读取，不整篇载入：先 `rg -n "^#{1,3} " docs/PROJECT.md` 取得标题与行号索引，再只读本任务命中的节——任务启动读「Issue 状态与阻塞」与目标能力表行，实施中按需补读「当前限制」与涉及的 ADR；仅全文审计类任务整读。其余长期文档同样只读任务涉及的章节。
- 专题实施基线只在首次处理相关专题、基线版本变化或当前任务涉及对应章节时读取。长期有效内容进入 `docs/PROJECT.md`、ADR、`CONTEXT.md` 词汇表或验收/架构文档；单次实施结果靠 Git 提交与 Issue 追溯，不复制到现行文档。
- 现行文档（PROJECT.md / ADR / CONTEXT.md）与历史追溯（Git / Issue）分开：只有长期有效内容进入现行文档；历史过程（按日期开发经过、提交清单、测试数字）与已发生的阶段事实不写回现行文档，也不单独维护开发记录文档，靠 Git 提交与 GitHub Issues 追溯。
- 产品文档定义了不等于程序已经实现；文档中的“已实现”以源码、自动化测试和实际界面共同验证为依据，编译通过仅表示构建成功。

### 文档写作

- 产品文档只书写当前有效的共识、边界、流程、依赖、验收标准和待确认事项。
- 使用现在时和正向表达，直接说明产品是什么、如何运行、包含哪些能力以及交付范围。
- 当前范围使用“当前 MVP 包含……”“当前输入范围是……”“本阶段交付……”等方式表达。
- 已确认内容与待确认事项分开书写；待确认内容明确标记为“待确认”或“候选方案”。
- 历史讨论、替代方案和演变过程放入独立归档，当前共识正文保持单一、清晰。

## 任务定义与协作上下文

- 任何功能新增、功能修正、缺陷修复、重构、产品决策或其他项目调整，在进入实施、排期或创建票据前，必须形成足够自洽的任务描述；描述要让第一次读到相关文字的模型或协作者不依赖历史聊天，也能直接理解任务。
- 每个任务描述至少明确以下内容：
  1. **做什么**：用户可见结果、具体行为、影响范围、明确不做什么；
  2. **为什么做**：触发背景、当前问题、用户痛点或产品目标；
  3. **应该做成什么样**：目标流程、数据或接口变化、交互形态、成功标准和关键示例；
  4. **提前避免什么**：已知 bug、边界场景、数据一致性要求、安全约束、回归风险和对应的规避方式；
  5. **受什么约束**：现有产品共识、技术边界、依赖、兼容要求、验证范围和暂不解决的事项。
- 任务描述应优先使用用户可见结果、具体输入输出、状态变化和可验证例子，不能只写抽象目标、模块名称、Issue 编号或实现步骤；关键术语、原有行为、不可破坏的关系和失败时的降级方式必须直接写出。
- 如果任务描述缺少上述关键信息，先补充上下文、提出待确认问题或明确记录候选方案，不直接根据猜测实现；实现过程中发现原始描述与产品意图不一致时，先更新任务和相关基线，再继续修改代码。
- 涉及"历史数据 / 旧数据 / 既有数据如何兼容或兜底"的判断时，先按 ADR-0007 开发数据兼容基线界定：开发数据期的历史研究数据可清空，不得作为产品或功能设计的约束条件，不因此增加兼容分支或降级兜底；只有用户显式批准数据兼容后才纳入考量。
- 每个纵向切片按可集成阶段推进。共享契约、迁移、持久化、HTTP/事件、任务恢复、确定性联调条件和对应验证满足集成前置条件后，再连接 WebUI 用户路径；只有说明或缺少真实可调用能力的接口不构成可集成基线。

## Codex 多 Agent 调度

项目级角色配置位于 `.codex/agents/`；模型和推理强度以 TOML 为唯一事实源。主 Agent 是 Orchestrator，负责目标理解、任务边界、依赖图、调度、冲突处理、集成、验证和最终交付。可选子角色为 `explorer`、`worker`、`architect_reviewer`。

### 动态选择

- **直接完成**：单一、边界清楚、低风险的文档/元数据/配置调整，或已定位的一处小修，由主 Agent 直接处理；不为展示流程而启动子 Agent。
- **Explorer**：代码位置未知、失败原因未知、涉及调用链/配置/日志、需要扫描三个及以上模块，或并行前需要建立事实地图时，先派只读 `explorer`。明确文件和调用链的问题可由主 Agent 直接查证。
- **Worker**：目标、验收标准和文件/模块所有权已经清楚，且存在实质编码、测试或常规重构工作时，派 `worker`。小改由主 Agent 直接做比交接更快时不派。
- **Architect / Reviewer**：涉及共享契约、API、数据库迁移、认证/凭证、安全、数据丢失风险、跨端公共接口、重大重构或不可逆技术取舍时，在实现前派 `architect_reviewer` 的 ARCHITECTURE 模式；高风险或跨边界实现完成后，再用新的独立上下文派 REVIEW 模式。普通低风险任务不强制调用。

### 编排流程

1. 把用户的最终目标改写为自洽任务：用户可见结果、原因、目标行为、不做什么、成功标准、风险与约束均明确；遇到未经确认的新产品概念，先执行上文强提醒而不是派 Agent 猜测。
2. 读取 `docs/PROJECT.md` 的目标能力、Issue 状态与阻塞，确认现有实现和未提交差异，再判断任务类型、影响面与验证等级。
3. 必要时让 Explorer 返回事实、入口、调用链、测试和未知项；主 Agent 基于证据拆出依赖图，标明串行前置与可并行叶子任务。
4. 只并行互不依赖且文件/模块所有权不重叠的任务。共享契约、迁移、公共配置、锁文件和同一核心文件的改动先串行确定，再释放下游任务；不得让多个 Worker 同时编辑同一范围。并行同一父 Issue 时读取 `docs/agents/parallel-development.md`，分配独立端口与完整共享上下文包。
5. 每份分派简报必须包含任务定义的五项内容、允许修改的文件/模块、依赖输入、预期输出、验证命令和明确不做项。子 Agent 只返回证据或自己负责的改动，主 Agent 负责跨任务整合与冲突裁决。
6. 主 Agent 检查完整差异并运行与风险匹配的真实验证。写入文件、编译通过或子 Agent 自报完成都不是完成证据。
7. 达到 Architect / Reviewer 门槛时，Reviewer 获得原始目标、完整差异、相关运行路径和验证证据，独立寻找遗漏；发现问题后由主 Agent 生成新的自洽修复任务，交给合适 Worker，重新执行受影响测试并复审。
8. 只有最终目标、验收标准、必要测试、文档/Issue 状态和差异范围全部一致时才结束。若外部服务、授权或未确认产品决定构成真实阻塞，报告证据和所需决策，不用猜测结果代替完成。

Agent 并发是容量上限，不是目标；优先减少交接、重复扫描和冲突。主 Agent 不把最终验收、Git 提交范围、Issue 关闭或面向用户的结论外包给子 Agent。

## Definition of Done

- 最终结果与任务描述中的用户可见行为、明确不做项和成功标准逐项对齐。
- 已确认修改前的真实实现，并检查 API、数据库、共享类型、配置或公共接口的全部相关上下游。
- 变更范围最小且复用现有实现；没有无关重构、技术栈替换、额外依赖或用户已有改动被覆盖。
- 按「分级验证」和 `docs/agents/testing.md` 运行了足以覆盖风险的真实命令，记录退出结果；未运行项有与风险匹配的明确理由。
- 用户可见 WebUI 改动按要求验证相关视口、键盘、可访问性、控制台、网络和必要视觉基线；真实模型与人类拟真验收只按各自专项触发规则执行。
- 检查 Git 差异和提交范围；回答“`docs/PROJECT.md` 四态能力表是否需要改动”，需要时同任务更新，关键难逆决策同任务写 ADR。
- 最终交付说明实际结果、修改文件、验证证据、残余风险和未完成项；只有证据支持时才使用“完成”。

## 代码提交

- 按结果提交，不按操作步骤提交。每个可验证、可审查、可独立回滚的任务边界或纵向切片完成并通过对应级别验证后，创建一次 Git 提交；不要把互不相关的目标累积到同一次提交。
- 连续产品讨论以一个完整决策分支作为提交边界。单次问答、单个琐碎边界确认或讨论中的逐次文档写回不触发提交；同一分支的已确认内容持续写入专题基线，在该分支形成自洽结论、会话收口或用户明确要求留档时统一检查并提交。
- 同一任务中的中间小修、记录同步、格式调整和验证记录默认并入该任务的实现提交，不单独提交。
- 有代码的常规工作通常使用一笔实现提交。纯复验通常不单独提交；发现阻塞或需要立即留档时，可以创建一笔状态记录提交。
- 安全修复、数据库迁移、构建配置等需要独立审查或回滚的变化按各自任务边界提交，不受常规笔数指导限制。
- 提交前检查差异与暂存范围，只提交本次任务相关文件，不覆盖或夹带用户已有的未提交修改。
- 每次提交前必须自问一句"`docs/PROJECT.md` 四态能力表是否需要因此改动"，并在提交信息或交付说明中写明答案——哪怕答案是"不需要，本次为同一已实现能力内部的修复"。能力状态、基线、活动父 Issue 或"当前限制与待产品决定"发生实际变化时，必须在同一次任务中同步 `docs/PROJECT.md`，不允许"先提交代码、文档以后再说"。
- 关键、难逆转、有真实取舍的产品或架构决策，必须在作出决策的同一任务中新增 ADR（`docs/adr/`），不允许只留在聊天记录或提交信息里。
- 任务对应完整 issue（不是纵向切片或拆分子票据）且提交完成后，必须在同一任务内及时关闭该 issue——用 `gh issue close` 附完成说明（用户可见结果、验收对照、提交/ADR/测试证据），不要留到后续任务；禁止在完成说明与 `docs/PROJECT.md` 四态能力表证据不一致时关闭。跨多笔提交、parent 规格、DEFERRED / SUPERSEDED 等情形的关闭时点与细则见 `docs/agents/issue-tracker.md`。
- 每个完成的实施阶段在提交信息与 Issue 中记录实际用户可见结果、实际改动、接口或数据变化、验证证据、与目标的偏差、已验证的风险、未完成项、后续边界和相关实现提交；不要把讨论中的候选方案或仅通过构建的结果写成已实现能力。用户可见 WebUI 阶段同时记录自动化浏览器、响应式、键盘、可访问性、控制台和网络验证结果；若用户主动触发人类拟真测试，再追加本次触发词、测试模式、功能区、复核结论和版本基线变化。

## 分级验证

- 验证范围按本轮实际改动和风险选择，默认执行能够覆盖风险的最小级别，不因“完成了一次修改”自动运行全量构建与全部测试。
- 一级（纯文档、文案、项目记录或元数据）：只检查格式、链接与引用、版本一致性、差异和提交范围；默认不运行代码构建、代码测试或浏览器测试。
- 二级（局部前端）：运行前端构建或类型检查及受影响的组件、客户端测试；用户可见交互变化增加 Playwright 自动化浏览器检查，覆盖相关视口、键盘、可访问性、控制台和网络；默认不运行无关后端全量测试，也不自动启动 Chrome DevTools MCP 人类拟真测试。
- 三级（局部后端）：运行后端构建或类型检查及受影响的单元、集成、API、持久化或重启恢复测试；只有影响用户完整路径或跨端契约时才增加浏览器验证。
- 四级（共享契约、迁移、构建配置、跨端集成或发布）：运行 `npm run gate` 全量门禁。
- 涉及认证、安全、数据丢失、数据库迁移、影响范围不确定，或本级验证失败时，至少提升一级；必要时直接执行四级验证。
- 任何合并进 `master` 的提交、任何跨分支共享的测试基础设施改动，必须先通过 `npm run gate` 全量门禁；进入实际实施的任务在写第一行业务代码前，必须按任务类型完成前置冒烟（纯文档、纯元数据等不进入业务实现的任务不触发）。门禁、冒烟与其余验证执行细节见 `docs/agents/testing.md`——凡涉及构建、测试或合并 `master` 的任务开工前必读。
- 自动化测试证明契约正确，是每个实施切片的常规完成条件；Chrome DevTools MCP “人类拟真测试”是低频的大版本体验验收，不是单切片完成条件，禁止在用户未主动触发时自动执行。触发词、三种模式、两条轨道、独立复核与报告格式见 `docs/agents/browser-acceptance.md`。
- 每个完成的开发阶段必须记录所选级别、选择原因、实际执行项及未执行项理由。按规则跳过无关检查是有效验证结果，不记为遗漏。

## Agent skills（专项规则路由）

- `docs/agents/testing.md`：验证执行细节——全量 Playwright 门禁、前置冒烟、迁移重放、快速失败、失败重跑、后台命令日志、失败分类报告、视觉基线审阅、确定性假模型响应清单。凡涉及构建、测试、合并 `master` 必读。
- `docs/agents/real-model-acceptance.md`：真实模型验收——模型配置读取、API Key 安全约束、探针先行、夹具自检、worktree 配置覆盖。
- `docs/agents/parallel-development.md`：并行开发——同迭代共享上下文包、并行端口分配、worktree 验收入口。
- `docs/agents/issue-tracker.md`：GitHub Issue 操作约定（`gh` CLI，仓库 `rappingzhuge/collector`）与 Issue 关闭细则。
- `docs/agents/domain.md`：领域文档布局与术语使用。
- `docs/agents/browser-acceptance.md`：人类拟真测试（Chrome DevTools MCP）唯一触发方式、三种模式与完整流程。
- `docs/agents/troubleshooting.md`：排障启发（建议级经验，如焦点类问题采样定位）。
