# AGENTS.md

本文件保存适用于 Collector 全部任务的长期约定。

## 用户沟通

- 默认使用中文。
- 用户不具备开发知识，先说明用户可见结果，再解释必要的技术原因。
- 使用产品语言描述界面、流程和风险；命令与内部模块名称只在排查需要时出现。
- 需要用户选择时，给出少量差异清楚的方案和推荐理由。

## 文档写作

- 产品文档只书写当前有效的共识、边界、流程、依赖、验收标准和待确认事项。
- 使用现在时和正向表达，直接说明产品是什么、如何运行、包含哪些能力以及交付范围。
- 当前范围使用“当前 MVP 包含……”“当前输入范围是……”“本阶段交付……”等方式表达。
- 已确认内容与待确认事项分开书写；待确认内容明确标记为“待确认”或“候选方案”。
- 历史讨论、替代方案和演变过程放入独立归档，当前共识正文保持单一、清晰。
- 文档中的“已实现”以源码、测试或实际界面验证为依据；编译通过表示构建成功。

## 多模型协作与交接

- 每次实施任务开始时先读取 `docs/handoffs/CURRENT.md`，按其中“本轮必读”列表加载增量上下文；不要默认重读全部长期文档。
- `docs/IMPLEMENTATION_COLLABORATION_PROTOCOL.md` 保存稳定协作规则。只有首次参与、协议版本变化或 `CURRENT.md` 明确要求时才完整重读。
- 角色实施基线只在首次接手、基线版本变化或当前任务涉及对应章节时读取。单次交接只记录相对已确认基线的变化，不复制长期产品说明。
- GPT-5.6 与 KIMI 3 每次转移开发责任时，交出方创建一份带唯一编号的增量交接文件，更新 `docs/handoffs/CURRENT.md` 和 `docs/handoffs/INDEX.md`，并与本轮代码一起提交。
- 交接文件必须说明用户可见结果、实际改动、接口或数据变化、验证证据、未完成项、风险、下一步和提交哈希。前端回交还必须包含浏览器、响应式、键盘、可访问性、控制台和网络验证结果。
- 接收方开始工作后，在本轮最终交接中标记已消费的上一份交接编号。交接状态没有更新时，不宣称责任已经完成转移。
- 稳定产品共识、协作协议、角色基线和单次实施记录分开维护。只有长期有效内容进入基线；本轮差异进入增量交接；历史摘要进入索引。
