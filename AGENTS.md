# AGENTS.md

本文件只保存 Collector 全部任务都需要遵守的长期规则。产品含义见 `docs/product/`；实施与验证细则见 `docs/ENGINEERING.md`；人工拟真验收见 `docs/ACCEPTANCE.md`。

## 用户沟通

- 默认使用中文。用户不具备开发知识时，先说明可见结果，再解释必要的技术原因。
- 使用产品语言描述界面、流程和风险；只在定位或验证需要时使用内部模块名和命令。
- 需要用户决定时，只提出会改变产品含义、授权范围或长期边界的问题，并附上冲突来源、无法自行判定的原因、各方案影响和推荐。
- 不要求用户确认普通实现细节；能够由现有共识、代码、测试或历史明确证据消解的问题直接处理。

## 产品边界与事实

Collector 以 `docs/product/README.md` 中的七个产品功能模块组织需求、术语和责任。模块契约只记录稳定的产品共识与明确不做项，不记录实现进度、日期、提交、Issue、测试数量或开发经过。

事实分工固定为：

1. 产品模块契约回答“产品允许什么、不得破坏什么”；
2. 源码、共享契约、迁移、自动化测试和实际界面回答“当前实现了什么”；
3. 当前任务描述回答“本次被授权改变什么”；
4. Git 与 GitHub Issues 只负责历史和例外任务追溯。

文档决定不等于已经实现；代码存在也不等于用户批准其产品含义。实现与产品契约冲突时，先判断是实现缺口、旧代码还是契约缺失，不静默改写任何一方。

代码和测试中遗留的 `ADR-xxxx` 只作为历史标签，不是现行文档入口；行为以相邻代码说明、测试和产品模块契约判断。

## 新概念与不确定性

- 新增或改变产品概念、功能、关系、状态、权限、自动行为或默认行为前，必须以“⚠️ 新概念 / 新功能 / 新边界提醒”单独说明并取得用户确认。
- 历史代码、旧文档、旧 Issue、原型和内部字段只能证明曾经存在，不能代替用户决定。
- 无法判断的内容保持“待确认”，不得写成现行共识、实施任务或验收标准，也不得继续推导依赖它的下游决定。
- 冲突可由更晚的明确用户决定和当前代码/测试共同消解时，记录为旧材料过时；仍有两种合理产品含义时才询问用户。

## 开始任务

- 先读 `docs/product/README.md`，再只读命中模块的契约；不要默认载入全部产品文档。
- 查询已实现行为时直接检查相关代码、测试与调用方。不要从产品契约、Issue 标题或提交说明推断实现状态。
- 涉及技术边界时读 `docs/ARCHITECTURE.md`；涉及编码、构建、测试、并行或合并时读 `docs/ENGINEERING.md`；只有用户主动触发人类拟真测试时读 `docs/ACCEPTANCE.md`。
- 开工前检查工作树和基线，保留用户已有改动。禁止为顺手整理引入无关重构、依赖或技术栈。

## 任务与协作

- 当前会话内可以完成的单一任务不创建 Issue、ADR、计划文件或进度文档；目标、非目标、不可破坏规则、依赖和验收保留在任务上下文与提交中。
- 只有跨会话、并行协作、长期阻塞、外部参与、真实缺陷、发布/安全/迁移或用户明确要求跟踪时，才创建或保留 GitHub Issue。
- 并行任务必须拥有明确基线、依赖、文件所有权、数据目录和端口；共享契约、迁移、认证、安全、锁文件、构建配置及同一核心文件串行整合。
- 子任务可以调查或实现明确范围；主任务负责产品边界、跨任务整合、完整差异、最终验证、提交范围和外部状态变更。
- 任何外部写入、Issue 关闭、分支处置、删除或其他难恢复副作用必须在用户授权范围内。

## 安全与数据

- WebUI 只经统一客户端访问 HTTP/渐进事件，不直接读取 SQLite、文件系统或模型凭证。
- API Key、认证头、会话令牌和启动器控制凭据不得进入普通日志、URL、浏览器持久化、业务导出、截图或 Agent 回传。
- 正文是内容唯一事实源；切片、语义范围、导航、引用、搜索索引和图谱均为派生或稳定定位层，不得建立可写正文副本。
- 开发期研究数据可以清理，不为其增加兼容分支；供应商配置、API Key、模型路由和设置必须保留，除非用户明确授权删除。
- `node_modules/`、`dist/`、`dist-tests/`、`.collector-data/`、`.npm-cache/`、`logs/`、`test-results*/`、`e2e-artifacts/` 是依赖或运行产物，不手工修改或纳入任务提交。

## 完成与记录

- 按改动风险执行足以覆盖风险的最小真实验证；共享契约、迁移、跨端集成、安全和合并到 `master` 按 `docs/ENGINEERING.md` 升级。
- 完成意味着用户结果、明确不做项、数据与安全边界、相关上下游、测试证据和实际差异一致；写入文件或编译通过本身不算完成。
- 按可验证、可审查、可独立回滚的结果提交。提交前检查完整 diff 和暂存范围，不夹带用户已有或无关改动。
- 普通功能完成不更新产品契约；只有产品含义发生已确认变化才修改对应模块。技术依赖变化才修改架构，运行方式变化才修改 README。
- 只有跨模块、难逆转、存在真实替代方案且需要长期保留理由的决定，才新增低频决策记录；记录定稿后不维护进度。
- 最终交付说明实际结果、修改范围、验证证据、残余风险与未完成项；没有证据时不使用“已完成”。
