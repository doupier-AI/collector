# Collector

Collector 是围绕 AI 对话和内容阅读展开的认知研究工具。用户可以直接与 AI 对话，也可以导入文档阅读；对任意可阅读内容的选区，Collector 提供引用（就地追问或深入研究这段）、来源返回能力，以及已完成的用户标记与笔记能力（GitHub issues #12/#13）。

AI 根据当前对话、当前内容、选区附近上下文和用户明确操作动态理解当下意图。

## Language

**本地 WebUI**：
Collector 的产品界面。用户双击启动器后，本机服务启动，并在默认浏览器打开 loopback 地址。数据、文件解析、AI 任务和凭证边界由本机服务负责。

**研究会话**：
承载用户与 AI 对话、当前打开内容和研究节点的连续使用空间。它可以从一次对话或一次文档导入开始。

**当前内容**：
用户此刻正在阅读的内容，可以是 AI 回答、导入文档或研究分支生成的内容。

**动态意图**：
AI 根据最近的用户表达、当前会话、当前内容和选区上下文，临时理解的关注方向。动态意图服务于当前判断。

**内容快照**：
为来源返回和关系追溯保存的稳定内容版本。

**选区智能窗口**（历史概念，H4a 起退役）：
原指选中文字后就近弹出的分析窗口，展示选区概括、AI 估计的学习成本与时间、前置知识等字段。2026-07-30 起整体退役：AI 分析字段（difficulty / quickReadMinutes / deepStudyMinutes / prerequisites / relationToFocus）为缺少判断依据的装饰性字段，选区动作收敛为选区引用胶囊。选区分析后端端点保留，前端入口不再触发。

**选区引用胶囊**：
用户选中文字后出现的轻量胶囊。当前实现（H4a）在输入框区域呈现引用态，提供「在此追问」（携带引用选区在当前节点对话流就地追问）与「深入研究这段」（以选区为来源生长子节点）两种发送模式。阶段 H 交互修订（issues #9–#11）将呈现位置改为选区上方浮动、取消选区字数下限，并把取消方式统一为点击选取文字以外的屏幕区域（Escape 不承担关闭职责）。

**选区质量提示**：
系统对过短、过长、残缺、跨越多个主题或难以可靠解析的选区给出的具体说明和调整建议。确定性规则承担第一层判断。“过短”提示已随 issue #10 退役（非空选区即有效）。

**AI 弱标记**：
AI 对当前内容中的术语、专有名词、缩写和其他短概念提供的低注意力提示。系统先理解当前内容，再从上到下逐步显示经过位置校验的标记。悬停约半秒判定为有意图后开始生成正式解释并在小弹窗中流式预览；点击标记即以该生成作为子节点的第一条消息，同一概念只生成一次，不重复生成。

**深入研究**：
用户从某个选区明确发起进一步研究的动作。系统保存来源内容、选区和位置，并从选区长出一个与来源双向关联的子节点（可选填写重点问题作为第一轮输入）。

**研究分支**（历史概念，已归并入研究节点）：
原指依附于某段来源内容的研究会话。H1 起数据层映射为 `research_nodes` 中的子节点，H2 起界面不再区分"分支"，统一为节点页。

**独立研究会话**（历史概念，已归并入研究节点）：
原指以当前选区开启的独立研究空间。H2 起"深入研究"二选一退役，收敛为单一的节点生长动作；旧数据作为带来源选区的根节点继续呈现。

**研究节点**（H1 数据层统一、H2 统一节点页与全屏树导航均已完成）：
统一的关系结构单元。一次对话或一篇导入文档可以成为根节点；每个节点功能完整，可以多轮追问、就地追问、生长子节点。节点本身没有类型差异；节点之间的关系自阶段 I（D1）起由类型化边表达——父子、语义相关、融合来源，其中父子关系是不可变的学习路径，语义相关与融合来源是附加关系。子节点从 AI 弱标记点击或手动选区生长，保留来源选区锚点。H1 已将 `research_sessions` / `research_branches` 映射为 `research_nodes` 中的根/子节点，消息、任务、选区、稍后再学统一归属 `nodeId`；H2 已将 WebUI 统一为 `/research/:sessionId/node/:nodeId` 节点页，旧会话/分支路由重定向到新地址。

**就地追问**：
引用当前节点内的一段选区，在同一节点内继续提问，不产生新的结构。与"开枝散叶"（选区长成子节点）是手动选区的两种去向。

**树状导航**：
在节点之间移动和总览关系结构的方式。MVP 形态为快捷键 `t` 唤出的全屏树视图，键盘可达；沉浸式 HUD 导航是后续候选实验方案。

**关系列表**（阶段 I · D1 已实现）：
快捷键 `g` 或顶栏按钮唤出的全屏导航呈现，与树视图共用同一服务端图投影，无第二份数据拼装。节点按关系类型（父子 / 语义相关 / 融合来源）分组呈现，纯键盘可达，屏幕阅读器可读。它是网状关系图导航的第一步，网状画布视图（D2/D3）待续。

**类型化边**（阶段 I · D1 已实现）：
节点之间带类型的关系，共三种：父子（由节点血统确定性派生、不可变的学习路径）、语义相关、融合来源。边持久化到 `research_edges`，创建幂等，刷新与重试不重复建立同一关系。语义边与融合边永不改写父子关系。

**语义切片**（阶段 I · E1 已实现）：
节点内容的结构化视图：一条回答在底层被切分为表达单一连贯命题的切片，但页面仍呈现为一篇连续长文，不做卡片碎片化。旧节点首次使用时按既有消息块边界惰性补齐确定性的临时切片，幂等、不改写历史。切片是后续切片感知上下文组装（E3）与融合引用（F）的存储与引用单元。

**融合节点**（候选概念，未实现，阶段 F）：
由两个或多个来源节点在核验相似后生成的新节点，内容保留共同点、各来源差异、综合推导与可回溯到来源切片的引用；来源节点逐字节不变，融合纯增量、可回溯。默认逐条确认，自动融合默认关闭。跨领域同名概念（两个“孙悟空”）默认判定为对比 / 联想而非同一实体。

**稍后再学**（历史概念，由用户标记与笔记接替）：
原指用户明确保存、附带一至五星优先级、希望以后重新关注的选区记录。旧体验（星级、AI 概括、待学/完成状态）过重，UI 入口随 H4a 选区智能窗口退役一并移除；数据底座 `research_later_items` 保留，语义调整为标记 + 笔记，见用户标记与笔记。

**用户标记与笔记**（已完成，issues #12/#13）：
用户对选区的轻量标记：点击【标记】立即保存选区引用，并可附一句自己想记下的笔记。标记保存在本机、不依赖 AI，可列表查看，可返回原文原选区位置。

**弱重现**（候选概念，未实现）：
原为稍后再学设计的自动提醒机制：依据用户设置、时间、内容相关性、冷却与主题轮换轻量展示保存项目并说明触发原因。是否随用户标记延续待确认。

**来源返回**：
从研究节点、用户标记或引用回到产生它的内容和具体选区位置。精确锚点可用时定位并高亮；其他情况下展示保存原文和粗粒度位置。

**行内引用胶囊**：
紧邻 AI 陈述显示的轻量可交互来源入口。点击后展开来源身份、时间、标题和相关摘要，并可进一步返回具体来源位置。

**内部文件阅读器**：
用户导入受支持文件后，在 Collector 内直接阅读和选取内容的只读界面。阅读器聚焦正文完整性、常见结构、可选取性和稳定来源返回。

**按需搜索**：
Collector 通过两条路径提供联网搜索能力：（1）模型供应商原生联网（OpenAI web_search、Gemini Google Search grounding、Anthropic server-side web search），（2）Agent 自主搜索循环（Bing/DuckDuckGo/Tavily/SearXNG，支持运行时切换和故障回退）。用户主动发起的 Chat、研究会话和分支追问在输入框提供联网开关，默认关闭；开启时才请求联网。选区分析不联网；其他 AI 生成场景由任务类型自行判断是否需要联网。

**Agent 搜索循环**：
Collector 自建的自主搜索机制。AI 模型可以发起搜索请求、查看搜索结果、决定抓取哪些页面、信息不足时换关键词再搜——形成一个"搜→看→判断→再搜"的循环，直到信息充足或达到上限。零新增依赖，通过手写 ReAct 循环实现。

**搜索后端**：
Agent 搜索循环可以切换使用的不同搜索引擎。当前支持四种：Bing（默认，零配置）、DuckDuckGo（免费回退）、Tavily（AI 专用搜索 API，需 Key）、SearXNG（自托管聚合引擎）。后端通过统一接口适配，支持运行时切换和故障回退。

**本地产品观测**：
Collector 在本机保存界面操作、状态变化、任务、模型调用和联网搜索的关联轨迹，用于定位产品问题和理解一次操作如何形成最终结果。

**模型会话轨迹**：
Collector 在模型网关边界记录实际提示内容、上下文、参数、流式回复、工具调用、耗时、用量、费用、重试和错误。记录关联研究会话、当前内容、选区和任务，并排除供应商凭证与认证令牌。

**人工验收说明**：
提供给普通使用者的逐步验收指南。它使用日常语言说明准备条件、操作动作、预期结果、失败记录、隐私提醒和停止条件。
