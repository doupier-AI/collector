# Collector 领域词汇表

本文件只定义领域语言，不描述实现状态、进度或计划。实现现状见 `docs/PROJECT.md`，决策留痕见 `docs/adr/`。

Collector 是具备自然普通对话能力的研究与学习工作空间。用户与 AI 对话、导入文档形成研究节点，通过显式生长构成思路脉络，并对多来源内容进行关联与融合。

## Language

**本地 WebUI**：
Collector 的产品界面。用户双击启动器后，本机服务启动，并在默认浏览器打开 loopback 地址。数据、文件解析、AI 任务和凭证边界由本机服务负责。

**统一对话空间**：
Collector 唯一的对话入口，打开即说话，无聊天/研究模式区分。普通对话是默认状态；研究节点的父子结构只由用户显式生长产生。（替代已退役的"聊天模式/研究模式"双入口概念。）

**会话**：
从一个根研究节点开始的管理与生命周期容器，可以包含用户显式生长出的多个研究节点。会话用于组织内容，不是关联发现或融合的知识边界。

**研究节点**：
由一段可持续多轮对话或导入文档形成的思路单元，也是研究地图中的稳定关系对象。普通追问与话题漂移不会自动拆节点；节点可以包含多个概念，其稳定性来自身份、来源和生长路径。

**生长**：
用户明确把当前内容扩展为新结构的动作。只有三种生长来源：深入研究、选区生长、弱标记生长。生长是创建子节点的唯一方式；生长动作零输入（不弹窗、不要求先起名）。

**节点焦点**：
节点的主题/标题。由系统自动从被生长内容提炼（弱标记文本或选区原文加当前问题），用户可随时编辑；自动提炼失败时用截断的来源文本兜底，不阻断生长。

**当前内容**：
用户此刻正在阅读的内容，可以是 AI 回答、导入文档或研究节点生成的内容。

**正文**：
节点或文档的连续可读内容，是唯一可读事实源。派生结构（语义范围、弱标记）失败只丢弃派生层，不影响正文阅读、复制、讨论和手动选择引用。

**内容解构**：
不改写原始内容，把稳定内容解析成可定位、可检索、可关联、可回溯的派生语义结构；解构产物绑定稳定内容版本。分三层：流式轻量提及标记；长文/跨节点按需语义范围索引；融合触发时的深度解构与关系核验。

**语义范围**：
正文内容的派生定位，是检索命中、上下文选择与融合引用的最小单位。用户不管理语义范围；它不受卡片边界限制，在检索时临时选取、在融合触发时深度核验。

**语义卡片**：
正文派生出的可替换呈现结构，只负责组织阅读，不是内容事实源、概念身份、搜索结果或融合边界。一个卡片可以包含多个语义范围，一个语义范围也可以跨越卡片呈现边界。

**保存时少判断，使用时再判断**：
Collector 的内容处理原则。消息产生时只保存并解析流内轻标记；检索时临时选语义范围；生长或融合触发时才做重解构。无关轮次允许存在，由消费端筛选排除。

**内容快照**：
为来源返回和关系追溯保存的稳定内容版本。

**选区引用胶囊**：
用户选中文字后在选区上方出现的轻量浮动胶囊，提供【引用】与【标记】。只有明确点击【引用】才进入输入框引用态；随后可以「在此追问」或「深入研究这段」。非空选区即有效，点击选区以外区域取消；Escape 不承担关闭职责。

**就地追问**：
引用当前节点内的一段选区，在同一节点内继续提问，不产生新的结构。与「深入研究这段」（选区生长为子节点）是手动选区的两种去向。

**深入研究**：
用户从某个选区明确发起进一步研究的动作。系统保存来源内容、选区和位置，并从选区生长出与来源双向关联的子节点。

**可解释对象**：
AI 判断为理解当前论述仍需补充解释的短范围，分为知识概念、命名实体、缩写、符号与技术标识四类；类别只描述对象性质，不表示该类对象的每次出现都必须形成提及。
_避免：名词类型、术语类型_

**AI 弱标记 / 提及**：
AI 对内容中的可解释对象提供的低注意力内联提示，即"提及"。目标形态：与正文在同一次模型流中产生，短语闭合即显示，用户永不见控制符，格式错误丢标记保正文。悬停约半秒开始流式预览；点击即以该预览生长子节点。

**语义实体**：
在同一研究节点的内容中保持稳定的概念身份，不等同于某一次提及、某条消息或某张语义卡片。同名对象只有经过当前节点内的语境核验，才共享同一语义实体身份。

**实体预览**：
悬停提及后流式生成的简短解释，也是点击生长时子节点的第一条消息；同一节点内经核验为同一语义实体的提及共享一份预览，不同节点各自拥有结合本节点上下文生成的预览。

**研究地图**：
研究节点、永久类型化边与可选关联提示层的只读投影，是节点系统的统一导航空间。所有未进入回收站的现存研究节点都属于研究地图的数据范围，归档不改变归属；显式生长只决定父子关系，不决定节点能否进入全局模式。

**图谱观察**：
用户从某个观察中心、内容范围、关系类型和节点状态角度查看同一套节点事实。全局模式、专注模式、搜索与筛选是观察条件的不同组合，不各自维护另一套图谱或关系解释。

**专注模式**：
研究地图在同一全局布局中的聚焦状态。焦点节点最突出；沿当前启用的永久关系可达的完整连通脉络保持正常色彩和明度，其余节点与连线保留在原位置并统一置灰。父子关系的结构与方向继续表达生长路径和生长树，进入或切换专注不重新布局。

**全局模式**：
研究地图覆盖全部未进入回收站的现存研究节点的呈现，归档节点仍在范围内。父子关系与融合来源构成永久事实底层，系统主动生成的关联提示可以作为明确区分的临时候选层叠加；聚类、抽稀和布局不改变全局数据范围。

**节点地址**：
研究节点在 Collector 内部的稳定定位，使用 `/nodes/:nodeId`，不依赖会话、项目、图谱布局、坐标或缩放。打开节点与以 `/map/focus/:nodeId` 进入专注模式是不同导航意图；节点地址不表示对外分享能力。

**图谱观察快照**：
用户从图谱打开节点正文时，为返回现场临时保存的当前节点位置、缩放和平移、观察中心、范围与关系筛选、状态叠加及本次临时观察历史。它只服务同一标签页的导航恢复，不是节点或关系事实，也不作为需要跨设备同步的长期知识数据。

**类型化边**：
节点之间带类型的永久关系。当前确认两种产品语义：父子（由节点血统确定性派生、不可变的学习路径）与融合来源。融合来源永不改写父子关系；关联提示和候选的永久语义关联都不构成当前确认的第三种类型化边。

**关系类型筛选**：
用户选择一种或多种永久关系作为当前观察路径。多种关系可以共同组成跨类型路径；命中的节点与连线增强，未命中的永久关系只弱化而不隐藏，避免观察角度被误解为关系事实消失。

**连通脉络**：
从当前选中节点出发，沿已选关系类型可以到达的全部节点与连线。连通判断允许从关系两端进入，不受父子或融合来源的语义方向限制；方向仍用于解释谁生长出谁、谁支撑了谁。连通脉络不设置用户可见的层数或距离上限；改变关系类型后重新计算，未连通区域保留在原位置并统一置灰。

**关联提示**：
AI 跨节点发现、用于帮助用户重新找到旧内容的临时提示。提示说明“为什么有关”，定位两端实际相关的语义范围，并可随证据或价值变化而过期；用户明确忽略后，只有新的实质证据改变理由时才可作为新提示出现。全局图默认不绘制提示；用户主动开启候选观察后才突出候选节点与连线，并明确标识为临时状态。提示不能升级为永久图谱关系，也不触发融合；融合由 AI 在内容稳定后独立判断是否形成了具体新增认识。

**永久语义关联（候选概念）**：
把“尚未形成融合、但用户希望长期反复沿线浏览”的普通关联保存为永久图谱事实的设想。它不同于临时的关联提示、记录思路血统的父子关系和记录实际来源的融合来源；讨论产品对象时避免单独使用含义不明确的“语义关联”或“语义相关”。

**融合提议**：
AI 在当前内容稳定后自主发现、围绕一条具有新增价值的综合认识组织的候选机会。一个提议可以引用两个或更多 A 面正式节点；同一来源集合可以形成多条不同提议。用户搜索、询问、对比、打开旧节点或明确要求融合都不触发提议。

**研究地图 A 面**：
研究地图中的正式知识底图，包含普通研究节点、正式融合节点及永久关系。B 面关闭时，用户在 A 面执行搜索、筛选、选择节点、改变观察中心、沿线探索和进入专注等常规图谱操作。

**研究地图 B 面**：
覆盖在 A 面同一画布上的临时融合观察层。它保留 A 面节点的位置作为只读来源背景，展示临时融合节点与候选来源连接；打开或关闭不会重新排列底图。B 面开启时 A 面常规操作关闭，原地查看证据仍属于 B 面，打开来源正文则退出 B 面并进入 A 面。

**临时融合节点**：
AI 已形成具体新认识、至少两个正式来源实际参与且关键证据可定位后自动创建的候选成果。它具有稳定身份、完整正文和普通对话，存在于研究地图 B 面，尚未成为永久知识事实；不能生长子节点、不能作为其他融合来源、不属于项目、没有归档状态。用户确认后，同一节点原位进入 A 面并成为正式融合节点。

**候选来源连接**：
只存在于 B 面的临时证据关系，连接临时融合节点与实际参与草拟的 A 面来源节点及语义范围。它不进入永久连通脉络；删除临时节点时一并删除，确认时只有最终采用且核验通过的连接转为正式融合来源。

**融合草案**：
由临时融合节点承载的候选内容，包含核心认识、来源差异或冲突以及判断与证据的对应关系。用户确认的是某个具体草案版本；确认只改变节点所在层与关系状态，该版本、实际采用的直接来源和证据范围原样成为固定的正式融合成果。

**草案版本**：
用户通过明确的对话修改指令产生的一次融合草案快照。版本可以比较和撤销；询问或讨论不会产生新版本，改变核心判断、来源或证据的版本必须重新核验受影响内容后才能确认。

**融合节点**：
由两个或更多来源节点的语义范围经核验后形成的独立会话根节点，表达用户在一个具体时点确认的一条新增综合认识。它通过融合来源边连接确认时实际采用的直接来源；融合正文、直接来源集合和判断与证据范围的对应在确认后固定。它拥有自己的多轮对话、生长树和生命周期，但后续对话、来源新增内容或其他状态变化不改写已确认成果。新的补充、纠正或替代性综合形成新的临时融合节点。全部直接来源属于同一项目时，融合会话默认归入该项目；来源跨项目时默认未分类，用户可以修改且不影响来源关系。

**来源节点**：
实际参与形成某个融合节点的直接输入节点。图谱以来源节点表达节点级来路，具体判断的证据仍由节点内的语义范围定位。
_避免：证据节点。_

**来源链**：
从融合节点沿直接来源递归展开得到的形成历史。默认直连只表达当前融合的直接来源，来源链保留多阶段综合的先后层次。

**来源暂不可用**：
融合来源所在会话进入回收站后的可恢复状态。来源节点暂不进入普通研究地图与搜索，融合节点的来源位置保留“来源在回收站”占位；恢复会话后，原有来源边和证据引用自动恢复。

**来源不完整**：
融合来源被永久删除后，相关融合判断无法再完整回溯原文的状态。融合成果继续存在，但被删来源的内容不得暗中保留，来源链只显示“来源已删除”缺失标记。

**用户标记与笔记**：
用户对选区的轻量标记：点击【标记】立即保存选区引用，并可附一句笔记。保存在本机、不依赖 AI，可列表查看，可返回原文原选区位置。

**来源返回**：
从研究节点、用户标记或引用回到产生它的内容和具体选区位置。返回是只读的定位动作：精确锚点可用时定位并短暂高亮后自动消失，不创建选区状态、不触发选区引用胶囊；锚点不可恢复时诚实展示保存原文和粗粒度位置，绝不把错误匹配假装成精确命中。

**行内引用胶囊**：
紧邻 AI 陈述显示的轻量可交互来源入口。点击后展开来源身份、时间、标题和相关摘要，并可进一步返回具体来源位置。

**内部文件阅读器**：
用户导入受支持文件后，在 Collector 内直接阅读和选取内容的只读界面。

**按需搜索**：
Collector 的联网搜索能力，由用户逐次控制，默认关闭。两条路径：模型供应商原生联网；Agent 自主搜索循环。

**Agent 搜索循环**：
Collector 自建的自主搜索机制：AI 发起搜索、查看结果、决定抓取、信息不足换词再搜，直到信息充足或达到上限。

**本地产品观测**：
Collector 在本机保存界面操作、状态变化、任务、模型调用和联网搜索的关联轨迹。

**模型会话轨迹**：
Collector 在模型网关边界记录实际提示、上下文、参数、流式回复、工具调用、耗时、用量、费用、重试和错误；排除供应商凭证与认证令牌。

**人工验收说明**：
提供给普通使用者的逐步验收指南，使用日常语言说明准备条件、操作动作、预期结果、失败记录、隐私提醒和停止条件。

**项目**：
会话的第一层分组容器（单层，不嵌套），用于整理和筛选，不是知识关系边界。会话可归属某个项目或处于“未分类”；跨项目关联提示与融合来源可以存在，移动会话不会改写既有来源关系；删除项目不会删除其中的会话，其下会话回到未分类。

**回收站**：
软删除会话的存放处。删除会话后进入回收站，保留 30 天后自动彻底清理；期间可恢复或彻底删除。彻底删除不可恢复，会级联清除该会话的整棵研究节点树。

**归档**：
会话的一种状态（active / archived）。归档不改变内容、关系与研究地图归属；归档节点在列表和全局图中默认保留但弱化，选中后恢复正常可读强度并明确显示“已归档”。用户可以按活跃与已归档状态组合筛选。

**用户显式命名**：
用户手动改过的会话标题（置 `titleEdited` 标记）。显式命名后，自动标题（首问派生与模型提炼）永久让位，不再覆盖。

**产品取色板**：
Collector 全产品唯一的颜色取色源。任何界面元素需要取色时，一律从这套色板选取，不另起新色。色板按浅色 / 深色两套定义，同一语义角色（底座、品牌、状态、边框、图表、侧栏）在两套中各有对应值。当前生效的品牌主色为暖橙/琥珀系（ADR-0018）。语义角色与命名对齐 `tokens.css` 令牌，取色即取令牌，不直接散落硬编码。色板值见下表；新增语义角色需先在此登记再落入令牌。

浅色 / 深色对照：

| 语义角色 | 浅色 | 深色 |
| --- | --- | --- |
| Background 页面底色 | `#faf9f5` | `#262624` |
| Card 卡片 | `#f5f4ef` | `#2c2c2b` |
| Popover 弹层 | `#ffffff` | `#30302e` |
| Primary 品牌主色（暖橙） | `#c96442` | `#d97757` |
| Secondary 次要表面 | `#e9e6dc` | `#faf9f5` |
| Accent 强调表面 | `#e9e6dc` | `#1a1915` |
| Muted 弱化表面 | `#ede9de` | `#1b1b19` |
| Destructive 危险/破坏 | `#141413` | `#ef4444` |
| Border 边框 | `#dad9d4` | `#3e3e38` |
| Input 输入框底 | `#b4b2a7` | `#52514a` |
| Ring 焦点环（同品牌色） | `#c96442` | `#d97757` |
| Chart 1（暖橙深，亦作品牌 hover） | `#b05730` | `#b05730` |
| Chart 5（暖橙浅，亦作标记/later） | `#b4552d` | `#b4552d` |
| Project amber（项目色角色） | `#c96442` | `#d97757` |
| Project violet（项目色角色） | `#9c87f5` | `#9c87f5` |
| Project blue（项目色角色） | `#2a78d6` | `#5b9af5` |
| Project teal（项目色角色） | `#1baf7a` | `#34c98f` |
| Project rose（项目色角色） | `#a33b36` | `#ef4444` |
| Chart 2（紫） | `#9c87f5` | `#9c87f5` |
| Chart 3 | `#ded8c4` | `#1a1915` |
| Chart 4 | `#dbd3f0` | `#2f2b48` |
| Sidebar 侧栏底 | `#f5f4ee` | `#1f1e1d` |
| Sidebar primary 侧栏主按钮 | `#c96442` | `#343434` |
| Sidebar accent 侧栏强调 | `#e9e6dc` | `#0f0f0e` |
| Sidebar border 侧栏边框 | `#ebebeb` | `#ebebeb` |

注：本表为取色源与语义角色约定；`tokens.css` 现行令牌值以源码为准，二者对齐是持续目标。选区高亮、标记徽标、难度色（`--color-difficulty-*`）、网状图边色（`--color-edge-*`）等语义独立色沿用各自既有族谱，不在本板内。浅色 Destructive 板值 `#141413` 在语义上是墨色、非警戒红，而本项目破坏性操作需用红色族谱表达"危险"——故运行时浅色 `--color-danger` 取红 `#A33B36`，未采用该板值（是否调整待产品确认，见 ADR-0019）。

## 已退役概念

**聊天模式/研究模式**：双入口已取消，见统一对话空间（ADR-0002）。
**对话容器**：含义同时指向会话和研究节点，容易混淆；管理与生命周期使用“会话”，多轮思路与图谱对象使用“研究节点”。
**关联模式**：会话内关系画布的旧称；节点系统统一使用覆盖全部未进入回收站的现存节点的“全局模式”。
**选区智能窗口**：原选中文字后的分析弹窗，装饰性字段已退役，动作收敛为选区引用胶囊。
**研究分支 / 独立研究会话**：已归并入研究节点。
**稍后再学**：由用户标记与笔记接替。
**运行时质量检查**：整体退役；正文无条件展示保存，下游质量由融合时来源核验承担（ADR-0004）。
**生成时完整语义切片**：降为待验证技术方案，需对照实验后再定（ADR-0004）。
**弱重现**：原为稍后再学设计的自动提醒，是否延续待产品决定。
**融合修订**：原指正式融合节点确认后的版本化维护。ADR-0025 将正式融合正文、直接来源和证据对应固定；新的补充、纠正或替代性综合改为形成新的临时融合节点。
