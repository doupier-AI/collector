# 排障启发

本文件收集经验性排障做法，属建议级别，不作为硬约束。验证执行的硬性要求见 `docs/agents/testing.md`。

## 焦点类疑难先怀疑「谁抢了焦点」

Escape/快捷键失效类缺陷优先用 100ms 间隔 activeElement 采样探针定位焦点去向，一次采样即可定位，比逐层加固断言快一个数量级。

## 图片不可渲染环境的视觉审阅

环境无法渲染图片时如何完成视觉验证（含默认审图方法），见 `docs/agents/testing.md`「无图环境的视觉验证」。
