# Collector 当前实施交接状态

状态版本：`21`

最后更新：2026-07-20

## 当前责任

| 项目 | 当前值 |
| --- | --- |
| 当前纵向切片 | 文件导入进入研究会话（WebUI 接收完成，下一步待决） |
| 当前责任角色 | GPT-5.6（H-011 已接收） |
| 下一接收角色 | KIMI 3 |
| 协作状态 | `implementing` |
| 待消费交接 | `none` |
| 最近已消费交接 | `H-20260719-011`（2026-07-20 已接收） |

## 当前有效基线

| 基线 | 版本 | 本轮是否变化 |
| --- | --- | --- |
| 多模型协作协议 | `COLLAB-PROTOCOL 1.3.0` | 无变化 |
| WebUI 前端实施基线 | `FRONTEND-BASELINE 1.6.0` | 无版本变化；仓库当前状态已同步文件导入与阅读恢复 |
| 后端研究会话契约 | SQLite migration v16 | 无变化 |
| WebUI 首个切片 | commit `5db1a85` + 界面调整 `9a3ad62` | 无变化 |
| WebUI 同源生产服务 | commit `709246b` | 无变化 |
| 浏览器测试收敛 | commit `07ae609` | 已实现：Playwright 直连 API 同源，场景通过且命令自然退出 |
| 文件导入后端 | commit `15f2f48` + 安全补强 `ee01685` + Markdown 结构补强 `fe562aa` | 已实现并达到 `frontend-ready` |
| 文件导入 WebUI 与阅读恢复 | commit `6479a37` + 2026-07-20 接收修复（未提交） | 已接收：真实上传、进度、取消、重试、幂等恢复与同画布阅读视图；会话切换隔离、SSE 401 停止和断线终态收口边界已补齐 |

## 本轮必读

GPT-5.6 继续当前责任时按顺序读取：

1. 当前对话上下文；
2. 根目录 `AGENTS.md`；
3. 本文件；
4. `docs/handoffs/H-20260719-011-KIMI3-TO-GPT56.md` 第 10 节接收验收；
5. `docs/FRONTEND_IMPLEMENTATION_HANDOFF.md` 第 4、10.4、16 节。

只有需要核对实现细节时，再读取 H-011 第 3 节列出的前端文件与实现提交 `6479a37`。不要求默认重读 H-001 至 H-010、全部长期文档或前端基线其他章节。

## 本轮边界

- 文件导入 WebUI 切片已接收；会话内附件入口是真实上传，开始页没有会话上下文，附件按钮继续保持占位；
- 接收时补齐三项前端边界：切换会话后旧上传结果和待重试文件不进入新会话；研究消息与导入 SSE 确认 401 后立即停止并进入重新配对；断线确认查询拿到终态时立即更新任务并停止重连；
- 共享契约、HTTP/SSE 服务端、SQLite v16、Cookie 与会话/消息幂等边界本轮未变化；
- 当前工作区另有一组未提交的启动器运行版本、旧实例替换和数据目录单服务锁改动。它不属于 H-011，代码与文档尚未形成可交付提交，真实启动/替换运行态尚未验证，不计入本切片已实现范围；
- 不调用真实云模型。

## 当前阻塞与责任

- 文件导入与阅读恢复无已知代码阻塞；
- 下一产品决定仍需在三个候选中选择：接近 20 MiB 的浏览器性能目标、Markdown 列表结构渲染策略、或进入基线第 16 节下一纵向切片；
- 服务重启中的导入恢复由后端集成测试覆盖（running 转 `failed/service_restarted` 可重试，queued 继续），未做浏览器级复现；
- Chrome DevTools MCP 未配置，现有 Playwright 控制台和网络检查仍为正式自动化证据；
- 启动器未提交改动的真实旧实例替换、PID 退出、数据目录锁释放与异常路径属于独立 `pending` 工作，不阻塞 H-011 接收。

## H-011 接收验证

- 验证级别：四级；原因是接收审查发现会话隔离和认证恢复缺陷，修改了共享研究消息/导入 SSE 客户端，并影响跨端完整用户路径；
- 接收前复跑：`npm run test:web` 88/88、`npm run test:e2e` 22/22 通过；
- 修复后复跑：`npm test` 137/137、`npm run test:web` 99/99 通过；`npm run test:e2e` 22/22 Chromium 场景通过并自然退出，构建随完整测试与 e2e 通过；
- 新增回归范围：旧会话延迟上传结果隔离、旧会话待重试上传隔离、研究消息与导入 SSE 首次断线确认 401 后立即停止、确认查询返回终态后的即时收口，以及确认查询晚于轮询返回 401 或终态时仍正确收口；
- 浏览器证据沿用 H-011：上传请求头、导入 SSE、取消/失败/重试终态、阅读视图 320/768/1024/1440 视口、键盘路径、控制台与网络、界面/API/SQLite 一致性；
- 测试全部使用本地确定性 fixture 和既有假模型，没有真实云模型调用；
- 已知测试警告：pdfjs 在无可选 canvas 原生绑定时输出渲染警告，文本提取、页码锚点和当前范围仍通过；
- 未执行项：系统默认浏览器人工窗口验收；20 MiB 近限性能实测；服务重启浏览器级复现；独立启动器未提交改动的运行态验收。

## 下一步

- 在文件导入的三个候选决策中确定下一纵向切片；
- 独立审查并验证当前未提交的启动器单服务改动，确认真实运行态后再建立自己的实现与交接边界；
- 当前接收修复和状态文档尚未提交；提交时只纳入 H-011 接收相关文件，不夹带启动器、TDD 技能或用户原有工作区变化。
