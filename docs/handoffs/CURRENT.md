# Collector 当前实施交接状态

状态版本：`4`

最后更新：2026-07-18

## 当前责任

| 项目 | 当前值 |
| --- | --- |
| 当前纵向切片 | 启动并恢复一场研究会话 |
| 当前责任角色 | GPT-5.6 后端与产品框架 |
| 下一接收角色 | KIMI 3（下一次前端实施交接） |
| 协作状态 | `backend_in_progress` |
| 待消费交接 | 无 |
| 最近已消费交接 | `H-20260717-002` |

## 当前有效基线

| 基线 | 版本 | 本轮是否变化 |
| --- | --- | --- |
| 多模型协作协议 | `COLLAB-PROTOCOL 1.1.0` | 无变化 |
| WebUI 前端实施基线 | `FRONTEND-BASELINE 1.2.0` | 已收入用户确认的界面方向与实际工程结构 |
| 后端研究会话契约 | SQLite migration v14 / commit `e4ce72e` | 无变化 |
| WebUI 首个切片 | commit `5db1a85` + 界面调整 `9a3ad62` | 已接收；用户路径与 12 项浏览器场景断言成立 |

## 本轮必读

GPT-5.6 继续后端与产品框架工作，按顺序读取：

1. 根目录 `AGENTS.md`；
2. 本文件；
3. 实施具体后端任务时，只读取该任务涉及的架构、契约和源码；需要核对 WebUI 契约时读 `docs/FRONTEND_IMPLEMENTATION_HANDOFF.md` 第 10、19 节。

`H-20260717-002` 已消费，不再列为日常必读。首次读取 `FRONTEND-BASELINE 1.2.0` 或需要核对界面基线时，只读第 7、8 节；不要求重读历史交接与全部长期文档。协作协议版本未变化。

## GPT-5.6 可以立即开始

- 最高优先级：让 WebUI 生产构建由本机 API 同源提供，正式运行不再依赖 Vite 代理；
- 第二优先级：完成启动器到首次 WebUI 的安全配对引导；
- 后续依次处理统一启动命令、会话创建幂等、文件导入契约和供应商原生流式；
- 下一次把前端责任交给 KIMI 3 时，要求修正 Playwright WebServer 收尾不退出的问题，并复跑能够自然退出的端到端命令。

## 当前阻塞与责任

- WebUI 生产同源静态服务未实现前，正式使用仍需 `dev:api` 加 Vite dev/preview 双进程；这是当前最高优先级阻塞；
- 其余 H-20260717-001 第 6 节阻塞项保持不变，已并入回交第 7 节任务清单；
- KIMI 3 本轮未发现新增后端契约缺口；
- 接收复跑中 12/12 浏览器场景断言均完成，但 Playwright 在终止第一套 WebServer 时未自然退出。该问题不影响当前后端开发，下一次前端实施前必须纳入修正与验证。

## GPT-5.6 完成前提醒

- 创建下一份递增编号的 `GPT56-TO-KIMI3` 增量交接；
- 更新本文件的当前责任角色、待消费交接和本轮必读；
- 更新 `INDEX.md`（本交接生效后可将 `H-20260717-002` 标记为 `superseded`）；
- 说明基线变化是 `none` 还是具体版本升级；
- 列出真实运行的命令、通过、失败和跳过项；
- 将代码、测试和交接文档一起提交，并报告提交哈希。
