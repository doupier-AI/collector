# H-YYYYMMDD-NNN-FROM-TO

状态：`prepared`

日期：YYYY-MM-DD

从：`GPT-5.6` 或 `KIMI 3`

到：`KIMI 3` 或 `GPT-5.6`

纵向切片：填写当前切片

消费的上一交接：填写编号或 `none`

## 1. 本轮结论

用三至六条说明用户现在可以看到或完成什么，以及本轮是否完整完成。

## 2. 范围

### 已完成

- 填写本轮真实完成内容。

### 未完成

- 填写没有完成或只部分完成的内容。

### 明确非目标

- 填写本轮没有计划处理的内容。

## 3. 提交与文件

- 分支：
- 提交哈希：
- 主要修改区域：
- 新增或升级依赖：
- 保留的其他工作区变化：

不要粘贴完整 diff。只说明下一角色需要重点审查的文件和原因。

## 4. 契约、数据和行为变化

- 共享类型：
- HTTP/SSE：
- SQLite/迁移：
- 路由和组件状态：
- 幂等、重试和恢复：
- 安全与隐私：
- 与上一基线的差异：

无变化的项目写 `none`，不要复制原基线。

## 5. 验证证据

- 所选验证级别：`一级 / 二级 / 三级 / 四级`
- 选择原因：说明改动类型、影响范围和是否触发升级条件。

| 检查 | 命令或方式 | 结果 | 跳过或限制 |
| --- | --- | --- | --- |
| 构建 |  |  |  |
| 单元/集成测试 |  |  |  |
| 浏览器测试 |  |  |  |
| 多视口 |  |  |  |
| 键盘/可访问性 |  |  |  |
| 控制台/网络 |  |  |  |
| API/SQLite |  |  |  |
| 项目检查 |  |  |  |

- 真实云模型调用：`否` / `是，附授权与范围`
- 测试产物或截图位置：
- 未执行项：逐项说明是按分级规则跳过，还是受环境或阻塞限制。

## 6. 已知问题与风险

每项包含：现象、复现步骤、影响、临时绕行、建议责任角色。

## 7. 需要接收方处理

按优先级填写下一角色可以直接执行的任务。需要接口变化时写出期望输入、输出和错误语义。

## 8. 基线变化

- 协作协议：`none` 或版本变化；
- 角色基线：`none` 或文件、旧版本、新版本、变化章节；
- 产品/架构/验收文档：`none` 或变化列表；
- 是否需要完整重读：
- 最小必读文件和章节：

## 9. 回滚与恢复

- 安全回滚方式：
- 需要保留的数据或迁移注意事项：
- 不可直接回滚的部分：

## 10. 接收验收

接收方确认后填写：

- 接收日期：
- 接收状态：`accepted` / `blocked`
- 复跑检查：
- 发现差异：
- 下一交接预计编号：

## KIMI 3 回交补充项

当交出方是 KIMI 3 时，必须额外填写：

- 用户完整操作路径；
- 每个页面状态；
- 浏览器和全部要求视口；
- 截图或测试产物；
- 键盘、焦点、可访问名称、标题层级和动态通知；
- 控制台错误/警告和关键网络请求；
- SSE 重连、去重和最终状态；
- 刷新、关闭和重新打开恢复；
- WebUI 动作对应的 API/SQLite 结果；
- 前端发现的后端缺口和复现步骤；
- 临时兼容层、假数据、技术债和不得合并内容。
