# H-20260718-005：KIMI 3 交给 GPT-5.6

状态：`accepted`

日期：2026-07-18

从：`KIMI 3`

到：`GPT-5.6`

纵向切片：WebUI 与本机 API 同源生产服务及浏览器测试收敛

消费的上一交接：`H-20260718-004`

## 1. 本轮结论

- Playwright 不再启动两套 Vite preview，两个项目直连有模型（43211）和无模型（43212）API 同源服务，12 项场景断言全部通过；
- 原唯一阻塞解除：`npm.cmd run test:e2e` 在 12/12 场景完成后自然退出，退出码 0，不再停在 WebServer 终止阶段；
- 一次性同源冒烟脚本在真实 Chromium 中确认：根页面、SPA 路由回退、`/assets` 静态资源、`/v1` 请求和 SSE 全部来自同一 origin，配对后控制台 0 错误/警告，0 个非本 origin 请求；
- API 测试进程支持 SIGTERM/SIGINT 优雅关闭（关闭 HTTP server 与 store，5 秒兜底强制退出），冒烟脚本实测 SIGTERM 后自然退出；
- 产品页面、界面方向、API 契约和持久化逻辑零改动；本轮任务全部完成，无遗留前端阻塞。

## 2. 范围

### 已完成

- `apps/web/e2e/api-harness.mjs`：把 `apps/web/dist` 作为 `webRoot` 传给 `createApiServer`，页面、静态资源、`/v1` 与 SSE 由同一 API 进程同源提供；缺少 `dist/index.html` 时启动阶段明确报错提示先构建；新增 SIGTERM/SIGINT 处理——停止配对码补充定时器、`closeAllConnections`、关闭 server 与 store，5 秒未关闭则强制退出；
- `apps/web/playwright.config.ts`：删除两套 Vite preview WebServer 及其 `COLLECTOR_API_ORIGIN` 环境变量；`chromium` 项目 baseURL 直连 `http://127.0.0.1:43211`，`chromium-nomodel` 直连 `http://127.0.0.1:43212`；测试进程从 4 个降为 2 个；
- `apps/web/e2e/helpers.ts`：`apiPortForPage` 按同端口推断（页面端口即 API 端口：43211 fake / 43212 nomodel），注释同步更新。

### 未完成

- 无。H-004 第 7 节五项要求全部完成。

### 明确非目标

- 不修改 React 组件、样式、路由或客户端状态；
- 不实现会话创建幂等、文件导入、设置页、稍后再学或供应商原生流式；
- 不处理启动器动态端口、重复启动复用与首次安全配对引导（仍属 GPT-5.6 队列）；
- 不调用真实云模型。

## 3. 提交与文件

- 分支：`master`；
- 提交哈希：`07ae609`（代码）；交接文档为相邻独立提交；
- 主要修改区域：`apps/web/e2e/api-harness.mjs`、`apps/web/playwright.config.ts`、`apps/web/e2e/helpers.ts`；
- 新增或升级依赖：`none`；
- 保留的其他工作区变化：`docs/INPUT_SOURCE_FEASIBILITY.md`、`docs/INTERACTION_DESIGN.md`、`docs/INTERFACE_DIRECTIONS.md`、`docs/PRODUCT_FUNCTION_FLOW.md`、`docs/PRODUCT_REFOUNDATION.md` 的用户原有修改未进入提交；
- 建议重点审查：`apps/web/e2e/api-harness.mjs` 的优雅关闭路径（`closeAllConnections` + `store.close` + 5 秒兜底）和 webRoot 缺失报错。

## 4. 契约、数据和行为变化

- 共享类型：`none`；
- HTTP/SSE：`none`（e2e 进程行为与正式服务一致：同源提供 dist；`/health` 检查路径不变）；
- SQLite/迁移：`none`；
- 路由和组件状态：`none`；
- 幂等、重试和恢复：`none`；
- 安全与隐私：`none`；配对码池、HttpOnly Cookie 换取流程不变，令牌不进入 URL、浏览器存储或日志；
- 与上一基线的差异：浏览器测试从「Vite preview 代理 + 环境变量指定 API」切换为「API 同源直连」，与生产运行方式一致。

## 5. 验证证据

- 所选验证级别：`二级`（前端测试基础设施），并按跨端集成风险补足构建与真实浏览器端到端验证；
- 选择原因：改动限于 e2e 测试配置与 harness，不涉及共享契约、迁移、产品组件或构建产物逻辑；存在用户可见启动路径的跨端验证已由浏览器场景与同源冒烟覆盖。

| 检查 | 命令或方式 | 结果 | 跳过或限制 |
| --- | --- | --- | --- |
| 构建 | `npm.cmd run build`（经 `test:e2e` 入口） | 通过；产出 `dist/index.html` 与 hashed assets | 无 |
| 单元/集成测试 | `npm.cmd run test:web` | 61/61 通过（10 个测试文件） | 根 `npm test`（122 项 Node）按分级规则跳过：本轮无后端代码变化，`709246b` 已经 GPT-5.6 四级验证 |
| 浏览器测试 | `npm.cmd run test:e2e` | 12/12 通过，命令自然退出，`EXIT_CODE=0`（原阻塞解除） | 无 |
| 多视口 | 既有 320/768/1024/1440 场景复跑 | 通过，无横向溢出 | 沿用既有截图用例 |
| 键盘/可访问性 | 既有键盘场景复跑 | 通过 | 未执行人工屏幕阅读器与 200% 缩放，本轮无新交互 |
| 控制台/网络 | 场景内断言 + 同源冒烟脚本 | 配对后 0 error/warning/pageerror；冒烟 21 个请求全部同一 origin，0 外部请求 | 配对前 401 属预期引导 |
| API/SQLite | 既有「界面、API 与 SQLite 记录一致」场景复跑 | 通过；消息、任务、事件表与界面一致 | 无迁移 |
| 项目检查 | `check-project.ps1` | 通过 | 无 |

- 真实云模型调用：`否`；
- 测试产物或截图位置：`.collector-data/same-origin-smoke-kimi/`（一次性冒烟脚本、`smoke-result.json`、`same-origin-webui-kimi.png`，已 gitignore 不入库）；`apps/web/e2e-artifacts/` 既有视口截图（已 gitignore）；
- 未执行项：根 `npm test` 按分级规则跳过（原因见上表）；人工屏幕阅读器与 200% 缩放不属于本轮测试基础设施改动风险。

## 6. 已知问题与风险

1. **单独运行 `apps/web` 的 `playwright test` 前需先构建**：harness 启动时检查 `dist/index.html`，缺失会明确报错；根 `npm.cmd run test:e2e` 总是先构建，不受影响。
2. **Vite preview 收尾路径不再使用**：若后续因任何原因恢复 Vite preview 方案，需要重新验证 Windows 下 WebServer 终止行为。
3. **SIGTERM 处理在 Windows 依赖 Playwright 的终止方式**：实测 `test:e2e` 自然退出且冒烟脚本 SIGTERM 后 harness 正常关闭；POSIX 环境未实测，预期行为一致（信号处理路径为标准 Node API）。

## 7. 需要接收方处理

- 审查本轮回交并接收；
- 按基线顺序推进后续切片；启动器首次安全配对引导、动态端口与重复启动复用仍在 GPT-5.6 队列（见前端基线第 19 节）；
- 无需接口变化，未发现新的后端缺口。

## 8. 基线变化

- 协作协议：`none`；
- 角色基线：`none`（`FRONTEND-BASELINE 1.3.0` 不变；本轮执行的是基线第 19 节既定任务）；
- 产品/架构/验收文档：`none`；
- 是否需要完整重读：否；
- 最小必读文件和章节：本交接全文；提交 `07ae609` 的 diff（3 个文件，约 40 行变化）。

## 9. 回滚与恢复

- 安全回滚方式：`git revert 07ae609`；
- 需要保留的数据或迁移注意事项：无数据库迁移；回滚后 e2e 恢复四进程模式与原收尾不退出问题，产品运行不受影响；
- 不可直接回滚的部分：`none`。

## 10. 接收验收

- 接收日期：2026-07-18
- 接收状态：`accepted`
- 复跑检查：`check-project.ps1` 通过；`npm.cmd test` 完成全量构建，121 项通过、1 项因 Windows 沙箱限制按预期跳过、0 失败；`npm.cmd run test:e2e` 在允许 Playwright 清理子进程树的权限下 12/12 通过，34.9 秒完成并自然退出，退出码 0
- 发现差异：受限沙箱中的首次 `test:e2e` 复跑在 12 项场景全部通过后，因 Windows 进程树终止能力被拦截而超时；在具备正常清理权限时与交接记录一致。Windows 下 Playwright 使用系统进程树清理，不依赖 harness 的 `SIGTERM` 处理；该处理仍覆盖手动信号关闭路径。未发现产品、HTTP/SSE 或 SQLite 行为差异
- 下一交接预计编号：`H-20260718-006`

## KIMI 3 回交补充项

- **用户完整操作路径**：与上一回交一致（配对 → 开始页 → 提交问题 → 渐进生成 → 完成 → 刷新/重开恢复），本轮无产品行为变化，全部场景在 API 同源地址上复跑通过；
- **页面状态**：无新增或修改；空、加载、失败、401、模型未配置等状态沿用既有实现与测试；
- **浏览器和视口**：Chromium；320/768/1024/1440 场景复跑通过；冒烟截图 1280×720；
- **截图或测试产物**：`apps/web/e2e-artifacts/`（视口截图）、`.collector-data/same-origin-smoke-kimi/`（同源证据），均已 gitignore；
- **键盘、焦点、可访问名称、标题层级和动态通知**：既有键盘场景（侧栏收起/展开、输入、发送）复跑通过；本轮无新交互；
- **控制台错误/警告和关键网络请求**：配对后 0 error/warning；关键请求为 `GET /`（document）、`GET /research/new`（SPA 回退 document）、`GET /assets/*`、`POST /v1/pairings/exchange`、`GET /v1/research-sessions`、`POST /v1/research-sessions/:id/messages`、`GET /v1/research-tasks/:id/events`（SSE）、`GET /v1/research-tasks/:id`，全部来自 `http://127.0.0.1:43319`（冒烟）或 43211/43212（e2e）同一 origin；
- **SSE 重连、去重和最终状态**：既有 sse-recovery 场景（生成中刷新、断线回退任务查询）复跑通过；
- **刷新、关闭和重新打开恢复**：场景 3（刷新恢复）与场景 4（关闭重开恢复最近会话）通过；
- **WebUI 动作对应的 API/SQLite 结果**：场景 9 通过——界面文本与 API 视图、SQLite 消息/任务/事件记录一致；
- **前端发现的后端缺口和复现步骤**：无新发现；
- **临时兼容层、假数据、技术债和不得合并内容**：`.collector-data/same-origin-smoke-kimi/smoke.mjs` 为一次性验证脚本，仅作本轮证据，不入库；无产品代码技术债。
