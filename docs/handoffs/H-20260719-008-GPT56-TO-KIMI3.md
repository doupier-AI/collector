# H-20260719-008：GPT-5.6 交给 KIMI 3

状态：`accepted`

日期：2026-07-19

从：`GPT-5.6`

到：`KIMI 3`

纵向切片：会话创建幂等完成与文件导入前端准备

消费的上一交接：`H-20260718-007`

## 1. 本轮结论

- 会话创建幂等已端到端完成：首次创建响应丢失后，用户重试会恢复已经保存的同一会话，不产生重复空会话；
- 创建请求、API、SQLite migration v15 和 WebUI 重试共用稳定的创建幂等语义；
- v14 既有研究会话升级到 v15 后完整保留，新会话才保存创建幂等键；
- 四级验证全部通过：128 项 Node 测试、62 项 WebUI 测试、14 项 Chromium 场景与项目检查均通过；
- KIMI 3 可以接收并审查本轮前端变化；文件导入前端只做正式契约允许的能力，不在后端契约就绪前伪造上传成功。

## 2. 范围

### 已完成

- WebUI 为一次会话创建意图生成稳定创建键，结果不确定后的重试复用原键；
- API 客户端和 `POST /v1/research-sessions` 使用必填 `Idempotency-Key`；
- SQLite migration v15 添加创建键与非空唯一索引，在事务中返回首次创建记录；
- 增加组件、API、并发、重启、v14 升级和真实浏览器响应丢失测试；
- 更新架构、前端实施基线和当前状态文档。

### 未完成

- 文件导入进入研究会话的正式共享契约、HTTP 路径、任务状态和恢复语义尚未在本轮实现；
- 文件选择、拖放、上传进度、失败恢复和阅读入口尚未进入 WebUI。

### 明确非目标

- 不实现会话列表分页、自动标题或供应商原生流式；
- 不修改启动器控制凭据、HttpOnly Cookie 或 Host/Origin 边界；
- 不在正式文件导入契约就绪前添加假上传成功、前端本地伪记录或猜测接口。

## 3. 提交与文件

- 分支：`feature/research-session-creation-idempotency`；
- 提交哈希：`5a06700`；
- 主要修改区域：`apps/api/src/http.ts`、`apps/api/src/research.ts`、`apps/api/src/store.ts`、`apps/web/src/api/client.ts`、`apps/web/src/features/research-session/StartPage.tsx`、相关组件/API/SQLite/Playwright 测试；
- 新增或升级依赖：`none`；
- 保留的其他工作区变化：用户原有 5 份未提交文档 `docs/INPUT_SOURCE_FEASIBILITY.md`、`docs/INTERACTION_DESIGN.md`、`docs/INTERFACE_DIRECTIONS.md`、`docs/PRODUCT_FUNCTION_FLOW.md`、`docs/PRODUCT_REFOUNDATION.md`，不得恢复、覆盖、格式化、暂存或提交。

## 4. 契约、数据和行为变化

- 共享类型：`ResearchSessionRecord` 无字段变化；创建幂等键属于 HTTP 与持久化元数据，不进入公开会话记录；
- HTTP/SSE：`POST /v1/research-sessions` 要求 `Idempotency-Key`，缺失或空白返回 400，长度上限 200；成功和幂等重放均返回 201 与同一 `ResearchSessionRecord`；SSE 无变化；
- SQLite/迁移：migration v15 为 `research_sessions` 增加可空 `creation_idempotency_key` 与非空唯一索引；既有行保持 `NULL`；
- 路由和组件状态：`StartPage` 在组件生命周期内保存创建键，成功导航后组件卸载；首次消息继续使用独立消息幂等键；
- 幂等、重试和恢复：同一创建键的串行、并发及服务重启后请求均返回首次会话；重试标题不覆盖首次标题；
- 安全与隐私：创建键不替代认证，不进入 URL、Cookie 或浏览器存储；原有本地认证与来源校验保持不变；
- 与上一基线的差异：`FRONTEND-BASELINE 1.4.0` → `1.4.1`，SQLite migration v14 → v15。

## 5. 验证证据

- 所选验证级别：`四级`；
- 选择原因：同时修改数据库迁移、HTTP、持久化、Web 客户端和真实用户恢复路径。

| 检查 | 命令或方式 | 结果 | 跳过或限制 |
| --- | --- | --- | --- |
| 构建 | `npm.cmd test` 内含完整构建 | 通过 | 无 |
| 单元/集成测试 | `npm.cmd test` | 128/128 通过 | 无 |
| WebUI 测试 | `npm.cmd run test:web` | 62/62 通过 | 无 |
| 浏览器测试 | `npm.cmd run test:e2e` | 14/14 Chromium 场景通过并自然退出 | 无 |
| 多视口 | 完整 Playwright 套件 | 320/768/1024/1440 无横向溢出 | 无 |
| 键盘/可访问性 | 完整 Playwright 套件 | 通过 | 无 |
| 控制台/网络 | 完整 Playwright 套件 | 无非预期错误；创建和消息请求均带各自幂等键 | 无 |
| API/SQLite | API、并发、重启、v14 升级及浏览器落库检查 | 通过 | 无 |
| 项目检查 | `powershell -File .agents/skills/collector-engineering/scripts/check-project.ps1` | 通过 | `-ExecutionPolicy Bypass` 形式被安全模式拒绝，当前执行策略下运行同一脚本通过 |

- 真实云模型调用：`否`；
- 测试产物或截图位置：`e2e-artifacts/`；
- 未执行项：无。

## 6. 已知问题与风险

1. 文件导入正式契约尚未实现：当前附件按钮只明确提示后续版本提供；KIMI 3 不得把该占位改成伪上传成功；责任角色为 GPT-5.6 先明确共享契约与后端能力，随后由 KIMI 3 接入真实 WebUI；
2. Chrome DevTools MCP 未配置：系统默认浏览器面板读取仍受限；现有等价证据来自隔离 Playwright 的控制台、网络、响应式、键盘和 SQLite 检查；
3. migration v15 已验证升级但数据库迁移不可通过简单代码回退撤销；回退前必须保留数据库备份并继续由兼容代码读取新增列。

## 7. 需要接收方处理

1. 按第 5 节最小验证核对提交 `5a06700` 的前端创建键复用行为，并把本交接标记为 `accepted` 后开始开发；
2. 审查 `StartPage` 在网络结果不确定、快速双击和成功导航时的状态边界，保持创建键与首条消息键相互独立；
3. 文件导入前端开始前，先核对 GPT-5.6 提供的正式共享类型、HTTP 状态、大小/格式限制、进度、失败、取消、重试和恢复语义；
4. 实现真实文件选择、拖放、上传进度、失败恢复与进入研究会话路径时，覆盖多视口、键盘、焦点、可访问性、控制台、网络及 API/SQLite 一致性；
5. 回交 GPT-5.6 时创建 H-009，完整填写 KIMI 3 前端补充项。

## 8. 基线变化

- 协作协议：`none`（恢复执行 `COLLAB-PROTOCOL 1.1.0` 的每次责任转移正式交接流程）；
- 角色基线：`docs/FRONTEND_IMPLEMENTATION_HANDOFF.md`，`1.4.0` → `1.4.1`，变化章节为第 4、10.2、19 节；
- 产品/架构/验收文档：`docs/ARCHITECTURE.md` 更新会话创建与消息提交幂等事实；
- 是否需要完整重读：否；
- 最小必读文件和章节：根目录 `AGENTS.md`、`docs/handoffs/CURRENT.md`、本交接、`docs/FRONTEND_IMPLEMENTATION_HANDOFF.md` 第 4、10.2、14、19 节、提交 `5a06700` 的前端文件与测试。

## 9. 回滚与恢复

- 安全回滚方式：优先 `git revert 5a06700`；若数据库已经升级，不应直接运行不认识 v15 的旧代码；
- 需要保留的数据或迁移注意事项：保留 `collector.sqlite` 和用户文件；v14 既有会话在 v15 中创建键为 `NULL`；
- 不可直接回滚的部分：SQLite 已执行的 migration v15，不通过删除列或索引进行现场回退。

## 10. 接收验收

接收方确认后填写：

- 接收日期：2026-07-19
- 接收状态：`accepted`
- 复跑检查：`npm.cmd run test:web` 62/62 通过（含“结果不确定后重试复用同一个创建幂等键”）；`npm.cmd run test:e2e` 14/14 通过（含“创建响应丢失后重试恢复同一会话”“快速双击发送只创建一个任务”“界面、API 与 SQLite 记录一致”）；`npm.cmd test` 128/128 通过；项目检查脚本未复跑，因本次接收只核对前端创建键复用行为且交付方四级验证已通过
- 发现差异：无；`StartPage` 在组件生命周期内复用创建键、成功导航卸载后生成新键，首条消息使用独立消息幂等键，快速双击由 `creatingRef` 拦截，与交接描述一致
- 下一交接预计编号：`H-20260719-009`
