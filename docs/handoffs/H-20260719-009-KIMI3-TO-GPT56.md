# H-20260719-009：KIMI 3 交给 GPT-5.6

状态：`accepted`

日期：2026-07-19

从：`KIMI 3`

到：`GPT-5.6`

纵向切片：会话创建幂等前端接收与文件导入正式契约请求

消费的上一交接：`H-20260719-008`

## 1. 本轮结论

- H-008 已接收并验收为 `accepted`：会话创建幂等的前端边界与交接描述一致，未发现差异；
- 接收复跑全部通过：62/62 项 WebUI 测试、14/14 项 Chromium 场景、128/128 项 Node 测试；
- 本轮没有代码或界面变化，只有交接状态与验收记录更新；
- 文件导入前端未开始：正式共享契约与后端能力尚未就绪，按约定不把附件占位改成伪上传成功；
- 请求 GPT-5.6 交付文件导入进入研究会话的正式契约与后端能力，之后由 KIMI 3 接入真实 WebUI。

## 2. 范围

### 已完成

- 按 H-008 第 7.1 条执行接收方最小验证：复跑 `npm.cmd run test:web`、`npm.cmd run test:e2e`、`npm.cmd test`，全部通过；
- 审查 `StartPage` 在创建结果不确定、快速双击、成功导航时的状态边界，确认创建键与首条消息键相互独立；
- 将 H-008 标记为 `accepted` 并填写第 10 节；同步更新 `CURRENT.md` 与 `INDEX.md`。

### 未完成

- 文件选择、拖放、上传进度、失败恢复与进入研究会话路径：等待正式契约与后端能力；
- 附件按钮仍为占位提示。

### 明确非目标

- 不实现假上传成功、前端本地伪记录或猜测接口；
- 不修改启动器控制凭据、HttpOnly Cookie 或 Host/Origin 边界；
- 不实现会话列表分页、自动标题或供应商原生流式。

## 3. 提交与文件

- 分支：`feature/research-session-creation-idempotency`；
- 提交哈希：`a0bcaf9`（回交 H-009 请求文件导入正式契约，仅交接文档）；
- 主要修改区域：新建本交接文件、更新 `docs/handoffs/CURRENT.md` 与 `docs/handoffs/INDEX.md`；
- 新增或升级依赖：`none`；
- 保留的其他工作区变化：用户原有 5 份未提交文档 `docs/INPUT_SOURCE_FEASIBILITY.md`、`docs/INTERACTION_DESIGN.md`、`docs/INTERFACE_DIRECTIONS.md`、`docs/PRODUCT_FUNCTION_FLOW.md`、`docs/PRODUCT_REFOUNDATION.md`，保持未暂存、未提交。

本轮无代码变化，无需要重点审查的源文件；接收结论与证据见 H-008 第 10 节和本交接第 5 节。

## 4. 契约、数据和行为变化

- 共享类型：`none`；
- HTTP/SSE：`none`；
- SQLite/迁移：`none`；
- 路由和组件状态：`none`；
- 幂等、重试和恢复：`none`；
- 安全与隐私：`none`；
- 与上一基线的差异：`none`（保持 `FRONTEND-BASELINE 1.4.1` 与 SQLite migration v15）。

## 5. 验证证据

- 所选验证级别：`一级`（本轮仅交接状态与元数据变化），另按 H-008 第 7.1 条执行接收方最小验证；
- 选择原因：无代码、契约或构建配置改动；一级默认不运行构建与测试，接收方最小验证是 H-008 明确要求的附加动作。

| 检查 | 命令或方式 | 结果 | 跳过或限制 |
| --- | --- | --- | --- |
| 构建 | `npm.cmd run test:e2e` 内含完整构建 | 通过 | 无 |
| 单元/集成测试 | `npm.cmd test` | 128/128 通过 | 无 |
| WebUI 测试 | `npm.cmd run test:web` | 62/62 通过，含“结果不确定后重试复用同一个创建幂等键” | 无 |
| 浏览器测试 | `npm.cmd run test:e2e` | 14/14 通过并自然退出 | 无 |
| 多视口 | 随完整 Playwright 套件 | 320/768/1024/1440 无横向溢出 | 无 |
| 键盘/可访问性 | 随完整 Playwright 套件 | 通过 | 无 |
| 控制台/网络 | 随完整 Playwright 套件 | 无非预期错误；创建与消息请求均带各自幂等键 | 无 |
| API/SQLite | 随“界面、API 与 SQLite 记录一致”场景 | 通过，含创建键落库断言 | 无 |
| 项目检查 | 未复跑 | 跳过 | 一级变更不要求；交付方四级验证已通过 |

- 真实云模型调用：`否`；
- 测试产物或截图位置：`e2e-artifacts/`；
- 未执行项：项目检查脚本按一级规则跳过，理由见上表；其余无。

## 6. 已知问题与风险

1. 文件导入正式契约未就绪：附件按钮只提示后续版本提供，用户无法把文件带入研究会话；无临时绕行；建议责任角色为 GPT-5.6，先明确共享契约与后端能力，再由 KIMI 3 接入 WebUI；
2. Chrome DevTools MCP 未配置：系统默认浏览器面板读取仍受限，沿用 H-008 的环境限制；现有等价证据来自隔离 Playwright 检查；
3. SQLite migration v15 不可通过简单代码回退撤销：回退前必须保留数据库备份，并继续由兼容代码读取新增列，沿用 H-008 第 9 节。

## 7. 需要接收方处理

按 H-008 第 6、7 节约定，文件导入前端开工前需要 GPT-5.6 明确并交付以下内容，KIMI 3 只在契约范围内实现：

1. 共享类型：`@collector/capture-contracts` 中附件/素材的记录类型、与研究消息或会话的关联方式和字段约束；
2. HTTP：上传路径、方法、请求与响应结构、大小与格式限制、错误码与面向用户的错误文案语义；是否要求幂等键及其作用域；
3. 任务状态：导入是否复用任务模型，进度、失败、取消、重试与服务重启后的恢复语义；
4. SQLite/迁移：需要的表、列或索引变化与迁移版本号，以及对既有会话的兼容性；
5. 阅读入口：导入完成后文件如何进入会话与恢复视图契约（快照字段、事件时序）；
6. 核对本交接后将其标记为 `accepted` 并开始契约工作；回交时创建递增编号 H-010。

## 8. 基线变化

- 协作协议：`none`（继续执行 `COLLAB-PROTOCOL 1.1.0`）；
- 角色基线：`none`（`FRONTEND-BASELINE 1.4.1` 保持不变）；
- 产品/架构/验收文档：`none`；
- 是否需要完整重读：否；
- 最小必读文件和章节：根目录 `AGENTS.md`、`docs/handoffs/CURRENT.md`、本交接、`docs/handoffs/H-20260719-008-GPT56-TO-KIMI3.md` 第 6、7 节、`docs/FRONTEND_IMPLEMENTATION_HANDOFF.md` 第 16、19 节。

## 9. 回滚与恢复

- 安全回滚方式：本轮仅交接文档变化，可 `git revert a0bcaf9`；代码侧回退仍按 H-008 第 9 节，优先 `git revert 5a06700`；
- 需要保留的数据或迁移注意事项：保留 `collector.sqlite` 和用户文件；v14 既有会话在 v15 中创建键为 `NULL`；
- 不可直接回滚的部分：SQLite 已执行的 migration v15，不通过删除列或索引进行现场回退。

## 10. 接收验收

接收方确认后填写：

- 接收日期：2026-07-19
- 接收状态：`accepted`
- 复跑检查：按一级验证核对 `a0bcaf9` 的文件范围、H-009 格式与引用、`CURRENT.md`/`INDEX.md` 状态一致性；本轮仅接收交接文档，未重复运行代码构建、代码测试或浏览器测试
- 发现差异：H-009 第 3、9 节原将本交接提交误写为 `c76ad30`，且第 3 节沿用了上一笔 H-008 接收提交的文件范围；实际 H-009 提交为 `a0bcaf9`，修改范围是新建本交接文件并更新 `CURRENT.md` 与 `INDEX.md`，接收时已更正。`c76ad30` 是上一轮接收 H-008 的交接提交。其余交接内容与验证记录一致，不阻塞接收。当前有效协作协议和前端基线已在 H-009 之后分别升级至 `COLLAB-PROTOCOL 1.2.0` 与 `FRONTEND-BASELINE 1.4.2`，以 `CURRENT.md` 为准，不属于 H-009 交付差异
- 下一交接预计编号：`H-20260719-010`

## KIMI 3 回交补充项

- 用户完整操作路径：本轮无界面变化；复跑覆盖既有路径——打开开始页、输入并提交、渐进内容进入同一消息并完成、刷新恢复、关闭后重开恢复、创建响应丢失后重试恢复同一会话；
- 每个页面状态：无变化，与 `FRONTEND-BASELINE 1.4.1` 一致；
- 浏览器和全部要求视口：Chromium；320、768、1024、1440 视口随 e2e 通过，无横向溢出；
- 截图或测试产物：`e2e-artifacts/`；
- 键盘、焦点、可访问名称、标题层级和动态通知：随 e2e 键盘场景通过；本轮无新增交互；
- 控制台错误/警告和关键网络请求：无非预期错误；创建请求带 `Idempotency-Key`，消息请求带独立消息幂等键；
- SSE 重连、去重和最终状态：无变化；SSE 中断回退任务查询场景通过；
- 刷新、关闭和重新打开恢复：对应 e2e 场景全部通过；
- WebUI 动作对应的 API/SQLite 结果：“界面、API 与 SQLite 记录一致”场景通过，含创建幂等键落库断言；
- 前端发现的后端缺口和复现步骤：文件导入正式契约未就绪，见第 6、7 节；无其他新发现；
- 临时兼容层、假数据、技术债和不得合并内容：无新增；附件按钮占位提示为既有约定，契约就绪前不得改成伪上传成功。
