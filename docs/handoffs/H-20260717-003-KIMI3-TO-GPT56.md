# H-20260717-003：KIMI 3 交给 GPT-5.6

状态：`pending`

日期：2026-07-17

从：`KIMI 3`

到：`GPT-5.6`

纵向切片：启动并恢复一场研究会话

消费的上一交接：`H-20260717-001`（`H-20260717-002` 为本方上一轮回交，尚未被接收）

## 1. 本轮结论

- 本轮是用户直接提出的 WebUI 界面方向调整，只改界面结构与样式，不碰数据流、API 客户端、SSE 与契约；
- 九项修改全部落地并通过验证（见第 2 节逐项记录）；
- 修改与前端基线 7.1–7.2 节的布局描述存在方向性差异（文字按钮→图标按钮、覆盖抽屉→默认展开固定侧栏、输入框结构调整），按冲突处理顺序以用户最新明确要求为准实施，是否收入基线留待 GPT-5.6 确认；
- 全部验证通过：完整构建、61 项前端单测、12 项 Playwright 端到端；后端回归 117/118（唯一失败为与本轮无关的既有计时竞争用例，见第 6 节）；
- 用户在 `H-20260717-002` 回交时尚未接收；本交接与 `H-20260717-002` 一并待 GPT-5.6 接收。

## 2. 范围（逐项如实记录）

### 已完成（用户确认的九项修改）

1. **左上"内容"文字按钮 → 图标按钮**：顶栏左侧 36×36 内联 SVG 侧栏类图标，`aria-label="内容"`，保留 `aria-expanded`/`aria-controls`；"内容"二字移至左侧栏展开后栏顶小标题；
2. **左侧栏初始展开**：宽屏（≥900px）为固定侧栏默认展开；窄屏为覆盖式抽屉默认收起（遮罩、Escape、焦点返回沿用）；
3. **开始页/输入区居中**：开始页标题、说明、输入框整体水平居中；
4. **取消"Collector"文字名 → 占位 logo**：88×88 内联 SVG（80×80 圆角方块 rx=21≈26% iOS 应用图标圆角，`--color-ai` 底 + 三条递减透明度白线），居中于 h1 上方；
5. **iOS 风格圆角令牌**：`--radius-control:10px` / `--radius-button:12px` / `--radius-input:18px` / `--radius-panel:20px` / `--radius-circle:50%`，全局替换旧圆角值；侧栏贴边无圆角；
6. **输入框内部精简**：仅保留占位提示"输入你想理解、比较或继续研究的问题……"；"Enter 发送，Shift+Enter 换行"移至框外下方小字（仍在 `aria-describedby` 中，满足可访问性界面说明要求）；可见标签"你的问题"转为 `sr-only`（无障碍关联保留）；
7. **右上"稍后再学"文字按钮 → 图标按钮**：书签类内联 SVG，`aria-label="稍后再学"`；原弹层提示已删除；
8. **右侧栏初始展开、内容暂不填充**：新增 `LaterPanel`（`complementary` 角色），栏顶小标题"稍后再学" + 空态"暂无内容"，仅体现展开/收起效果；
9. **侧栏拖拽调宽**：两固定侧栏内边缘 `SidebarResizeHandle`（Pointer Events + pointer capture），**默认 264px、最小 208px、最大 400px** 刚性钳制；`role="separator"` + `aria-valuenow/min/max`，方向键步进 16px；宽度存 React state 不持久化；钳制纯函数 `sidebar-width.ts` 配 10 条单测。

### 用户确认过程中的三点调整（覆盖初次清单）

- 发送按钮**留在输入框内**右下角：36px 圆形、`--color-ai` 底 + 白色向上箭头，与输入框圆角等距（约 10–12px），禁用态降不透明度；
- 输入框内左下角**预留"+"按钮位**：对称描边圆形按钮，`aria-label="添加附件（后续版本提供）"`，点击显示"附件等功能将在后续版本提供"（不用无反馈按钮）；
- 侧栏自定义拉伸及最小/最大刚性区域（即第 9 项）。

### 本轮附带修复（e2e 暴露的真实 UX 缺陷）

- **配对完成后最近会话列表不自动刷新**：侧栏默认展开后面板先于配对挂载，401 失败文案常驻；新增 `paired-event.ts`，PairingGate 配对成功派发事件，SessionListPanel 监听后自动重载。仅 UI 层协调，配单测。

### 未完成 / 非目标

- 右侧栏具体功能内容（用户明确暂不填充）；
- "+"按钮真实附件能力（占位预约，等待后端导入契约）；
- 侧栏宽度持久化（当前仅存内存，刷新回默认 264px）；
- 本轮不涉及数据流、契约与后端，其余非目标同 `H-20260717-002`。

## 3. 提交与文件

- 分支：`master`
- 代码提交：`9a3ad62`；交接文档提交：紧随其后的相邻独立提交；
- 主要修改区域：`apps/web/src/components/AppShell/`（重写 + 3 个新文件）、`apps/web/src/features/navigation/`（ContentDrawer 双模式、新增 LaterPanel）、`apps/web/src/features/chat-composer/ChatComposer.tsx`、`apps/web/src/features/research-session/StartPage.tsx`、`apps/web/src/styles/`（圆角令牌与布局）、`apps/web/e2e/research-session.spec.ts`；
- 新增/升级依赖：`none`；
- 保留的其他工作区变化：5 份用户原有修改状态的产品文档仍未提交；
- 建议重点审查：`AppShell.tsx`（双侧栏状态机与断点行为）、`sidebar-width.ts`（钳制）、`ChatComposer.tsx`（框内圆形按钮与预留位）、`paired-event.ts`（跨组件刷新协调）。

## 4. 契约、数据和行为变化

- 共享类型 / HTTP / SSE / SQLite：`none`；
- 路由和组件状态：AppShell 增加左右侧栏展开状态与宽度 state（内存）；断点 900px 区分固定/覆盖模式；窄屏一次只展开一个侧栏；
- 可访问性：图标按钮均有 aria-label；侧栏小标题语义保留；拖拽手柄 `separator` 角色可键盘操作；输入框提示仍在 `aria-describedby`；
- 与上一基线的差异：**前端基线 7.1 顶栏文字按钮与 7.2 覆盖抽屉描述已被用户新要求替代**；6.1"中央 Collector 名称"改为占位 logo；输入框结构（按钮内置圆形化）与基线 ASCII 不同。建议 GPT-5.6 确认后将新布局方向收入前端基线下一 Minor 版本。

## 5. 验证证据

- 所选验证级别：`二级`（局部 WebUI、样式、组件与交互改动）
- 选择原因：改动限于 `apps/web` 界面层，不涉契约/迁移/构建配置跨端集成；因含用户可见交互变化，按二级要求执行真实浏览器、多视口、键盘、控制台与网络检查；后端回归属额外复跑，非级别要求。

| 检查 | 命令或方式 | 结果 | 跳过或限制 |
| --- | --- | --- | --- |
| 构建 | `npm.cmd run build` | 通过 | 无 |
| 单元测试 | `npm.cmd run test:web` | 61/61（原 43 + 新增 18） | 无 |
| 浏览器测试 | `npm.cmd run test:e2e` | 12/12（新增开始页截图用例） | 无 |
| 多视口 | 320/768/1024/1440 断言 + 截图 | 通过；320/768 侧栏隐藏、1024/1440 双栏展开 | 无 |
| 键盘/可访问性 | e2e 键盘流（默认展开→收起→焦点保持→再展开）+ app-shell 单测（窄屏 Escape/焦点返回）+ 手柄键盘步进 | 通过 | 人工屏幕阅读器未执行 |
| 控制台/网络 | 沿用 14.2 断言（提交链路 console 干净、幂等键头、SSE content-type） | 通过 | 配对前 401 不计入 |
| API/SQLite | 沿用 14.2 第 14 条用例 | 通过 | 无 |
| 后端回归 | `npm.cmd test` | 117/118（见第 6 节既有计时竞争用例） | 本轮未改后端与 scripts |

- 真实云模型调用：`否`；
- 测试产物或截图位置：`apps/web/e2e-artifacts/viewport-{320,768,1024,1440}.png` + `start-page-1440.png`（已 gitignore）；
- 未执行项：人工屏幕阅读器与 200% 缩放人工复核（同 H-20260717-002）。

## 6. 已知问题与风险

1. **后端回归中 `scripts/ralph/runner.test.mjs`"timed out child processes cannot continue running"在高负载并行全套运行时失败（117/118）**：该用例断言 250ms 超时杀进程树后 900ms 内无孤儿写入，属计时竞争；单独运行（15/15）与串行全套（`--test-concurrency=1`，118/118）均通过，本轮两次基线全量运行（本轮改动前）也通过。文件不在本轮改动范围（未触碰 `scripts/`、后端），非本轮引入。建议 GPT-5.6 知悉该用例的负载敏感性。
2. **侧栏宽度不持久化**：刷新后回到默认 264px；如用户反馈需要记住宽度，再评估加入最小偏好存储。
3. **窄屏一次只展开一个侧栏**：打开另一侧会收起当前侧栏，属有意简化。
4. 其余 H-20260717-002 第 6 节事项（会话创建幂等、e2e 顺序依赖、控制台断言范围等）保持不变。

## 7. 需要接收方处理

1. 确认本轮界面方向（图标按钮 + 默认展开双侧栏 + 拖拽调宽 + 占位 logo + iOS 圆角 + 输入框结构调整）是否收入前端基线（建议 Minor 版本更新 7.1/7.2/7.3 节）；
2. `H-20260717-002` 第 7 节阻塞清单维持不变（同源静态服务仍为最高优先级）；
3. 无新增后端缺口。

## 8. 基线变化

- 协作协议：`none`；
- 角色基线：`none`（布局方向变化待 GPT-5.6 确认后收入，见第 7 节第 1 条）；
- 产品/架构/验收文档：`none`；
- 是否需要完整重读：否；
- 最小必读文件和章节：本交接全文；`H-20260717-002`（首个切片完整交付证据）；如需审查代码见第 3 节重点文件。

## 9. 回滚与恢复

- 安全回滚方式：`git revert 9a3ad62` 即可回到 `H-20260717-002` 的界面形态；无数据与契约影响；
- 需要保留的数据或迁移注意事项：无；
- 不可直接回滚的部分：无。

## 10. 接收验收

接收方确认后填写：

- 接收日期：
- 接收状态：
- 复跑检查：
- 发现差异：
- 下一交接预计编号：

## KIMI 3 回交补充项

- **用户完整操作路径**：与 H-20260717-002 相同；新增：点击顶栏左右图标按钮收起/展开对应侧栏；拖拽侧栏内边缘调宽（208–400px）；点击输入框"+"看到后续版本提示；
- **每个页面状态**：同 H-20260717-002；右侧栏为空态"暂无内容"；
- **浏览器和全部要求视口**：Chromium；320/768（覆盖式默认收起）/1024/1440（双栏展开）均通过无横向溢出；
- **截图或测试产物**：`viewport-{320,768,1024,1440}.png`、`start-page-1440.png`；
- **键盘、焦点、可访问名称、标题层级和动态通知**：图标按钮 aria-label、侧栏小标题、手柄 separator 键盘步进、窄屏 Escape 与焦点返回均有测试覆盖；
- **控制台错误/警告和关键网络请求**：配对完成后 console 无 error/warning；契约断言通过；
- **SSE 重连、去重和最终状态**：本轮未改动，H-20260717-002 证据仍有效（e2e 全量复跑通过）；
- **刷新、关闭和重新打开恢复**：本轮未改动，e2e 全量复跑通过；
- **WebUI 动作对应的 API/SQLite 结果**：一致性用例复跑通过；
- **前端发现的后端缺口和复现步骤**：无新增；
- **临时兼容层、假数据、技术债和不得合并内容**：侧栏宽度内存态；"+"按钮为占位预约；右侧栏空态为有意占位。
