# H-20260717-001：GPT-5.6 交给 KIMI 3

状态：`pending`

日期：2026-07-17

从：`GPT-5.6`

到：`KIMI 3`

纵向切片：启动并恢复一场研究会话

消费的上一交接：`none`

## 1. 本轮结论

- 首个研究会话后端纵向切片已经实现并提交；
- 用户输入、AI 占位消息和任务在模型调用前通过同一 SQLite 事务保存；
- 后端提供会话恢复、SSE 事件续传、无模型失败重试和服务重启恢复；
- 前端工程尚未创建，当前责任转交给 KIMI 3；
- 完整浏览器联调仍依赖 GPT-5.6 补齐同源静态服务和首次 Cookie 引导。

## 2. 提交与范围

- 后端提交：`e4ce72ebb9ba1df18a72481b8e83ef67988357c1`；
- 前端实施基线提交：`39a2168`；
- 主要后端区域：共享契约、研究服务、SQLite migration v14、HTTP/SSE、模型网关和研究会话测试；
- `apps/web` 未创建；
- 5 份用户原有修改状态的产品文档未包含在提交中。

## 3. 已实现契约

详细契约以 `docs/FRONTEND_IMPLEMENTATION_HANDOFF.md` 第 10 节和共享类型为准，本文件不重复字段全文。

接口范围：

- `GET/POST /v1/research-sessions`；
- `GET /v1/research-sessions/:id`；
- `POST /v1/research-sessions/:id/messages`；
- `GET /v1/research-tasks/:id`；
- `GET /v1/research-tasks/:id/events`；
- `POST /v1/research-tasks/:id/retry`。

关键语义：

- 消息提交必须使用 `Idempotency-Key`；
- SSE 事件是 `snapshot`、`delta`、`completed`、`failed`；
- 使用 `Last-Event-ID` 或 `after` 续传；
- `model_not_configured`、`provider_error`、`service_restarted` 可恢复；
- 数据路由需要本地 token，并校验 loopback Host 和同 Host Origin。

## 4. 验证

- `npm.cmd run build`：通过；
- `npm.cmd test`：118 项，117 项通过、0 项失败、1 项原有 Windows 沙箱检查跳过；
- Collector 项目检查：通过；
- 集成测试覆盖幂等提交、SSE、SQLite 重开、无模型重试、服务重启和安全来源；
- 真实云模型调用：否；
- 真实浏览器验证：未执行，前端尚不存在。

## 5. KIMI 3 可以立即开始

- 创建 `apps/web` 工程和前端构建；
- 实现研究画布、会话列表、Chat 输入和任务状态；
- 实现 SSE snapshot/delta/完成/失败和断线恢复；
- 完成空、加载、错误、离线和认证过期状态；
- 完成响应式、键盘、焦点、减少动态效果和可访问性；
- 建立客户端测试和 Playwright 骨架。

## 6. 当前后端阻塞

以下事项仍由 GPT-5.6 处理，KIMI 3 不自行绕过：

- API 同源提供 WebUI 生产静态资源；
- 启动器安全建立首次浏览器 Cookie；
- 统一前后端开发与假模型启动命令；
- 会话创建幂等；
- 文件导入进入研究会话；
- 真实供应商原生 token stream。

## 7. KIMI 3 回交要求

KIMI 3 完成后创建下一份 `KIMI3-TO-GPT56` 交接，并完整提供：

- 用户可见结果和未完成状态；
- 提交、文件、依赖和跨边界修改；
- 实际接口、SSE 和错误处理核对；
- 浏览器、全部视口、截图、键盘和可访问性证据；
- 控制台、网络、刷新和重新打开恢复结果；
- WebUI 操作对应的 API/SQLite 结果；
- 后端缺口、复现步骤和期望契约；
- 全部验证命令、通过、失败、跳过和真实云调用情况；
- 基线变化与下一轮最小必读列表。

缺少真实浏览器、接口或恢复证据时，只能回交为部分完成或阻塞。

## 8. 基线变化与本轮最小必读

- 协作协议：首次建立 `COLLAB-PROTOCOL 1.0.0`；
- 前端基线：建立 `FRONTEND-BASELINE 1.0.0` 版本标记；
- 本次是 KIMI 3 首次接手，需完整读取前端实施基线；
- 后续若基线版本未变化，只需读取 `CURRENT.md` 和新的增量交接。

最小必读列表以 `docs/handoffs/CURRENT.md` 为准。
